Build a production-quality multilingual language system for the QuotePro web app only.

Important:
- This is ONLY for the web app
- The iOS app already supports this and should be treated as the reference behavior/product intent
- The implementation must be robust, scalable, and executed cleanly
- The system must separate:
  1. the user’s app/interface language
  2. the language used for outbound customer-facing messages

SUPPORTED LANGUAGES

Add full support for these languages in the web app:
- English
- Portuguese
- Spanish
- Russian

Use proper locale-aware codes and architecture, for example:
- en
- pt
- es
- ru

PRODUCT REQUIREMENT

A user should be able to use QuotePro in one language, while sending customer communications in another language.

Example:
- the user’s QuotePro web app is in Spanish
- their customers are English-speaking
- all outbound messages to customers must be generated in English

This separation is critical and must be built intentionally throughout the product.

CORE CONCEPTS

There are 2 distinct language settings:

1. APP LANGUAGE
This controls the QuotePro web app UI language:
- navigation
- buttons
- forms
- labels
- onboarding
- dashboards
- empty states
- modals
- settings
- system messages
- templates shown inside the app

2. OUTBOUND CUSTOMER MESSAGE LANGUAGE
This controls the language for customer-facing communications generated or sent from QuotePro, such as:
- quote follow-up emails
- quote follow-up SMS/text
- appointment confirmations
- reminders
- reactivation messages
- sales outreach
- customer communication templates
- AI-generated outbound copy
- automated sequences
- any customer-facing message content

These two settings must be independent.

EXAMPLES OF REQUIRED BEHAVIOR

Example 1:
- app language = Spanish
- outbound customer message language = English
Result:
- user sees QuotePro UI in Spanish
- AI-generated follow-up texts/emails to customers are in English

Example 2:
- app language = Portuguese
- outbound customer message language = Portuguese
Result:
- app UI in Portuguese
- customer messages in Portuguese

Example 3:
- app language = Russian
- outbound customer message language = English
Result:
- app UI in Russian
- all customer-facing messages default to English

GOAL

Implement a flawless multilingual system for the web app that:
- supports localized UI
- supports independent outbound messaging language
- works across all key workflows
- is scalable for more languages later
- does not mix concerns
- does not hardcode strings everywhere
- does not create inconsistent translations across the app

ARCHITECTURE REQUIREMENTS

Design and implement this properly.

Use a localization / internationalization architecture that includes:
- centralized translation files
- locale-aware routing or app-level locale state
- translation hooks/utilities
- interpolation support
- pluralization support where needed
- date/number formatting support where appropriate
- fallback language behavior
- missing translation safeguards
- typed translation keys if possible

If the QuotePro web app already has a stack, use the best-fit i18n solution for that stack.
If starting from scratch or upgrading an incomplete system, prefer a production-grade solution appropriate to the framework.

If the app is Next.js / React, use a strong i18n setup compatible with the project architecture.
Keep the implementation clean and maintainable.

DO NOT:
- scatter translation strings inline across components
- duplicate logic
- mix app-language logic with outbound-message-language logic
- rely on brittle hacks

DATA MODEL / SETTINGS REQUIREMENTS

Implement distinct persisted settings for:

1. app_language
2. outbound_message_language

These should be stored per user and/or workspace/business as appropriate for the current QuotePro architecture.

Think carefully about scope:
- app language may be user-specific
- outbound message language may be workspace-level, business-level, or user-configurable default depending on product architecture
- customer-level language overrides may be useful and should be supported if feasible

At minimum, implement:
- user or account-level app language preference
- default outbound message language setting

Recommended enhancement:
- optional customer-level preferred language override for outbound messages

PRIORITY LANGUAGE LOGIC

Implement the following outbound message language resolution logic:

When generating or sending a customer-facing message, determine language in this order:

1. customer preferred language, if set
2. workspace/account default outbound message language
3. fallback to English

This should be centralized in one service/helper, not repeated all over the codebase.

WEB APP UI SCOPE

Localize the full web app UI for:
- auth/login screens
- onboarding
- dashboard
- navigation
- quotes
- leads
- customers
- pricing logic
- settings
- forms
- modals
- buttons
- notifications/toasts
- empty states
- loading states
- validation messages
- AI assistant UI if present
- any customer messaging screens/templates

SETTINGS UX

Create or update a language settings area in the web app where users can clearly manage both settings.

The settings UX should make the distinction obvious.

Example structure:

Language Settings

1. App Language
Description:
Controls the language you see inside QuotePro.

Options:
- English
- Portuguese
- Spanish
- Russian

2. Default Customer Message Language
Description:
Controls the default language used for emails, texts, reminders, and other customer-facing communications.

Options:
- English
- Portuguese
- Spanish
- Russian

Optional enhancement:
3. Customer Preferred Language
Set per customer record to override the default customer message language.

Important:
The UI copy should make it extremely clear that app language and outbound message language are separate.

OUTBOUND MESSAGE REQUIREMENTS

Any feature that generates customer-facing copy must respect outbound language settings.

This includes:
- manual message drafting
- templated messages
- AI-generated messages
- follow-up texts
- follow-up emails
- reminders
- quote delivery messages
- reactivation campaigns
- sales nurture messaging
- automated workflows
- customer communication previews

Wherever outbound messages are created, previewed, edited, or sent, the correct language must be used automatically.

Also provide the ability for the user to change the language manually per message if appropriate, without changing their global default.

Example:
- default outbound language is English
- on a specific message compose screen, user can switch to Spanish for one message only

This should be implemented thoughtfully if feasible.

AI / MESSAGE GENERATION REQUIREMENTS

If the app has AI-generated outbound messaging, update the AI prompting/orchestration so message generation respects the resolved outbound language.

Examples:
- if outbound language resolves to English, AI-generated copy must be in English
- if outbound language resolves to Portuguese, AI-generated copy must be in Portuguese

Important:
The AI should not confuse:
- the user’s app language
with
- the target language of the customer message

For example:
- user interface is Spanish
- user asks AI to draft a message to an English-speaking customer
- the UI should remain Spanish
- the generated message must be English

This distinction must be preserved in both backend orchestration and frontend UX.

TRANSLATION CONTENT REQUIREMENTS

Translate all core UI content professionally and consistently.
Avoid crude machine-translated phrasing.

The product voice should remain:
- clear
- modern
- concise
- operational
- premium

Be especially careful with:
- CRM terms
- quote/pricing terms
- cleaning-business terms
- sales/follow-up language
- settings descriptions
- validation errors
- onboarding text

If needed, create a translation dictionary / namespace structure such as:
- common
- auth
- navigation
- dashboard
- quotes
- leads
- customers
- pricing
- settings
- messages
- ai
- validation
- onboarding

COMPONENT / CODE REQUIREMENTS

Refactor the web app as needed so components use a consistent translation approach.

Implement:
- translation provider / locale context
- typed translation utility if possible
- reusable language selector components
- locale persistence
- SSR/CSR-safe language loading if relevant to the framework
- a shared helper for formatting localized strings
- message-language resolver service
- centralized outbound message generation language handling

If current code has hardcoded English text, replace it systematically.

DATE / NUMBER / FORMATTING

Localize relevant formatting where appropriate, including:
- dates
- times
- number formatting
- currency display if supported by existing architecture

Be careful:
- language preference and currency are not the same thing
- do not infer business currency solely from language

ERROR HANDLING / FALLBACKS

Implement safe fallback behavior:
- if a translation key is missing, fall back gracefully to English
- do not crash the app due to missing translations
- log or flag missing keys in development
- if outbound message language is missing/invalid, fall back to English

CUSTOMER DATA MODEL ENHANCEMENT

If feasible, add a preferred_language field to customer records with supported values:
- en
- pt
- es
- ru

Use this to override default outbound message language.

Update customer create/edit forms to support this field if added.

MESSAGE COMPOSER / TEMPLATES

Where customer-facing messages are composed:
- show the current target language clearly
- allow preview in the target language
- ensure template selection and generation respect the target language
- preserve final editable message text in that language

Example UI:
- “Message language: English”
- with option to change for this message only

AUTOMATIONS / SEQUENCES

If the web app has automated outbound messaging, update automation logic so it uses resolved outbound language too.

This includes:
- reminders
- drip/follow-up sequences
- reactivation campaigns
- quote nudges

Every customer-facing automation should use:
1. customer preferred language if available
2. default outbound message language
3. English fallback

TESTING REQUIREMENTS

Create thorough coverage for:
- UI language switching
- persistence of app language
- persistence of outbound message language
- correct separation of app UI language from customer message language
- customer-level preferred language override
- fallback behavior
- message generation in the correct language
- template rendering in the correct language
- AI-generated message language correctness

Add realistic test cases such as:
- app in Spanish + outbound English
- app in Russian + outbound Portuguese
- app in English + customer override Spanish
- missing translation key fallback
- message composer override for one-off message language

SEED / DEMO DATA

Seed the app with sample data showing multilingual usage:
- a Spanish-speaking user with English-speaking customers
- a Portuguese-speaking business with mixed-language customers
- at least one customer with preferred language override

This should make testing easy and demonstrate the system immediately.

README / DOCUMENTATION

Update the README with:
- localization architecture overview
- supported languages
- where translation files live
- how to add/edit translations
- how app language works
- how outbound message language works
- how customer-level language overrides work
- fallback behavior
- setup / migration instructions

QUALITY BAR

This must feel like a serious production-grade multilingual system, not a patch.
It should be clean, scalable, and robust.
The distinction between UI language and outbound message language must be flawless.

DELIVERABLES

I want:
1. full web-app-only implementation
2. app UI localized for English, Portuguese, Spanish, and Russian
3. separate outbound message language system
4. settings UI for both language types
5. customer preferred language override if feasible
6. AI/message generation updated to use correct target language
7. translation files / architecture
8. fallback behavior
9. seed/demo data
10. tests
11. updated README

START BY

1. audit the current web app for hardcoded strings and current language patterns
2. implement the i18n architecture
3. add app language persistence
4. add outbound message language persistence
5. add customer preferred language override
6. refactor the UI to use translation keys
7. update all customer messaging flows
8. update AI/message generation logic
9. add tests
10. update documentation