You are updating QuotePro’s UI design system + Home dashboard styling to feel like a premium “Revenue OS” (not a generic SaaS dashboard). Objective: stronger visual hierarchy, fewer competing accent colors, and a more confrontational (but still tasteful) Revenue Leak experience.

DO NOT change core navigation structure or remove features. This is a UI/UX + copy + theming pass.
Must render perfectly on iPhone + iPad (centered, no stretched cards, max content width).
Keep an Apple-native look: clean typography, consistent corner radius, subtle shadows, no neon, no clutter.

========================
1) COLOR SYSTEM (REDUCE CHAOS)
========================
Currently there are too many competing accents (blue, orange, green, beige, teal glow). Create a cohesive palette with strict rules:

PRIMARY (Brand / Trust)
- Use brand blue for primary actions, links, and key highlights.
- It should be the default CTA color app-wide (except “Urgency” CTAs).

URGENCY (Revenue at risk only)
- Use orange ONLY for revenue risk states and the primary “Stop the Leak” action.
- Orange should be “sacred”: do not use it on unrelated widgets.

SUCCESS / ACTIVE (Automation ON)
- Use green ONLY for “Engine ON”, success confirmations, and positive states.
- Avoid using green on revenue risk widgets.

NEUTRALS
- Replace the beige/sand revenue background with clean neutrals.
- Use light neutral backgrounds for cards; reserve tinted backgrounds only for state signaling.

Provide a single constants file (or theme config) with semantic tokens:
- colorPrimary
- colorPrimaryText
- colorUrgency
- colorUrgencyBg (very subtle tint)
- colorSuccess
- colorSuccessBg (very subtle tint)
- colorDanger (for severe risk only)
- colorCardBg
- colorPageBg
- colorTextPrimary
- colorTextSecondary
- colorDivider
- shadowCard
- radiusCard

If the app uses a theme system already, extend it; otherwise create one and refactor dashboard widgets to use semantic tokens (not hardcoded hex).

========================
2) DASHBOARD LAYOUT RULES (PREMIUM)
========================
- All cards should share consistent padding, corner radius, shadow, and typography scale.
- Enforce a max content width on iPad (e.g., 520–620px) and center the column.
- Remove/soften any extra glows (especially the teal bottom glow) if it competes with card hierarchy.
- Each card must have one “primary emphasis” element only: either a number OR a button, not both screaming.

========================
3) HOME SCREEN COPY UPDATES (REVENUE OS VOICE)
========================
Replace polite wording with revenue-authority wording (still professional, not aggressive).
Examples:
- “Revenue waiting to close” -> “Revenue at risk”
- “Follow Up Now” -> “Stop the Leak”
- “View Quotes” -> “Protect This Revenue”
- “Follow-up health” -> “Revenue Protection Score”

Keep copy concise and punchy.

========================
4) REVENUE LEAK DETECTOR CARD (REDESIGN)
========================
Redesign the first revenue card to read as a “Revenue Leak Detector” (money discomfort) rather than a generic follow-up reminder.

Card Header:
- Icon + label: “Revenue Leak Detector”
- Small state pill: “Cooling / Cold / Critical” based on oldest at-risk quote

Primary:
- “$939 at risk” (use urgency color for the $ number only, not the whole card)

Secondary:
- “2 quotes slipping • Oldest: 3 days”
- Add one line that creates discomfort:
  “If your close rate stays at 45%, you’re likely losing ~$516.”

Revenue Protection Score:
- Show as “Score: 0/100 (F)”
- Use a subtle progress bar with color based on score:
  90+ primary, 70–89 amber, <70 urgency

Actions:
- Primary button (urgency): “Stop the Leak” -> goes to Follow-Up Queue filtered to at-risk quotes
- Secondary text button (primary color): “See what’s leaking” -> opens Leak Details list

VISUAL STYLE:
- Replace the beige background with a clean neutral card background.
- Use a thin left border or top accent line that turns amber/orange based on risk state (NOT a full tinted background).
- Keep it looking premium and “executive”.

========================
5) SALES MOMENTUM CARD (TIGHTEN)
========================
- Ensure the numbers use a consistent color logic:
  Sent/View/Accepted/Won should not each be random colors.
- Use primary blue for the active metric, neutral for others, and success green only for “Won” or “Accepted”.
- Make the “Close rate” line feel like a KPI: bolder label, lighter value.
- No extra gradients.

========================
6) AI REVENUE ENGINE CARD (GREEN RULES)
========================
- Green indicates “ON” only.
- Keep the card background neutral; use green only for:
  - the “ON” label
  - the status dot
  - small accent icon
- Ensure “Manage Sequences” button uses primary blue unless it’s truly urgent.

========================
7) FOLLOW-UP STREAK CARD (GAMIFIED BUT CLEAN)
========================
- Keep streak dots subtle and consistent.
- Avoid adding new accent colors.
- Tie the streak visually to Revenue Protection Score (same typography scale).

========================
8) IMPLEMENTATION TASKS
========================
A) Create/Update theme tokens (semantic color constants).
B) Refactor the Home screen widgets to use semantic tokens (remove hardcoded colors where possible).
C) Update Revenue card copy + layout per above.
D) Adjust bottom nav styling to reduce competing glow; keep it clean and consistent with the theme.
E) Validate on iPhone + iPad simulators: no overflow, centered, legible.

========================
9) ACCEPTANCE CRITERIA
========================
- Home screen looks more premium and cohesive with fewer accent colors.
- Orange appears ONLY on revenue risk elements and the “Stop the Leak” CTA.
- Green appears ONLY for “Engine ON” and success states.
- Revenue card clearly communicates “at risk / leaking” with discomfort line and score + grade.
- iPad layout is centered with max width.
- No visual clutter, no neon, no excessive gradients.

Proceed to implement now. Show me the updated Home screen after refactor.