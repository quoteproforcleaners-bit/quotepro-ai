You are an expert senior mobile engineer and product engineer. You will upgrade my existing iOS app “QuotePro” (React Native + Expo preferred) into a production-ready quoting + automation platform for house cleaning businesses.

GOAL
Implement ALL features 1–6 below (Scheduling & Reminders, Automated Follow-ups, Proposal/Reporting, Booking+Payments, Spreadsheet Replacement/Operations, CRM) in a way that is:
- Extremely simple for cleaners to adopt (minimal setup)
- Works on iPhone (iOS) with a modern dark-mode UI consistent with existing app screenshots
- Monetizable via Free vs Pro tiers with feature gating
- Reliable with proper data modeling, background jobs, and message delivery tracking
- App Store compliant (privacy, permissions, disclosures)

TECH STACK (use this unless impossible)
- Mobile: React Native (Expo), TypeScript, React Navigation
- State: React Query (or similar) + Zustand (or simple Context)
- Backend: Supabase (Postgres + Auth + Storage + Edge Functions + cron) OR Firebase if you strongly prefer. Choose ONE and implement fully.
- Payments: Stripe (subscriptions + optional payment links/deposits)
- Messaging: Twilio (SMS) + SendGrid (email) OR Resend (email). Choose providers and wire them end-to-end.
- PDF generation: generate professional proposals/invoices as PDF (either client-side using a HTML-to-PDF approach or server-side via Edge Function). Must include logo + brand colors + terms + line items.

CORE ENTITIES / DATA MODEL
Create these tables/collections and relationships:

1) businesses
- id, owner_user_id
- company_name, email, phone, address
- logo_url (storage)
- brand_primary_color (default to current blue)
- sender_name, sender_title
- booking_link (optional)
- timezone
- created_at, updated_at

2) pricing_settings
- id, business_id
- hourly_rate
- minimum_ticket
- tax_rate
- frequency_discounts: weekly, biweekly, monthly
- service_types: array of {name, multiplier, description, active}
- package_mapping: good_service_type, better_service_type, best_service_type
- add_on_prices: inside_fridge, inside_oven, inside_cabinets, interior_windows, blinds_detail, baseboards, laundry_fold, dishes, organization_tidy, etc.
- created_at, updated_at

3) customers (CRM)
- id, business_id
- first_name, last_name
- phone, email
- address, notes
- tags (array)
- lead_source (optional)
- status (lead, active, inactive)
- created_at, updated_at

4) jobs (scheduled cleanings)
- id, business_id, customer_id
- job_type (touch_up, regular, deep, move_in_out, post_construction, airbnb_turnover, etc.)
- status (scheduled, completed, canceled)
- start_datetime, end_datetime
- recurrence (none/weekly/biweekly/monthly) + recurrence_rule fields
- assigned_team (optional, future)
- internal_notes
- created_at, updated_at

5) quotes
- id, business_id, customer_id (nullable for quick quote)
- property_beds, property_baths, property_sqft
- service_type_selected OR package_selected (good/better/best)
- frequency_selected (one_time/weekly/biweekly/monthly)
- add_ons_selected (array)
- subtotal, tax, total
- status (draft, sent, accepted, declined, expired)
- sent_via (sms/email)
- sent_at, accepted_at, declined_at, expires_at
- public_quote_token (for shareable web view)
- created_at, updated_at

6) quote_line_items
- id, quote_id
- name, quantity, unit_price, total_price
- type (base, add_on, discount, tax)

7) communications (automation + tracking)
- id, business_id, customer_id, quote_id (nullable), job_id (nullable)
- channel (sms/email)
- direction (outbound/inbound)
- template_key (followup_1h, followup_24h, reminder_24h, etc.)
- content
- status (queued, sent, delivered, failed)
- provider_message_id
- scheduled_for
- sent_at
- error_message

8) automation_rules
- id, business_id
- enabled
- quote_followups_enabled (boolean)
- followup_schedule: array like [ {delay_minutes: 60, template_id: ...}, {delay_minutes: 1440, ...}, {delay_minutes: 4320, ...}, {delay_minutes: 10080, ...} ]
- quote_expiration_days (default 7)
- job_reminders_enabled (boolean)
- job_reminder_schedule (default 24h before)
- review_request_enabled (optional, can be stubbed)
- created_at, updated_at

9) files
- id, business_id
- type (quote_pdf, proposal_pdf, invoice_pdf)
- quote_id (nullable)
- url
- created_at

AUTH + ONBOARDING
- Use email/password sign-in.
- First run onboarding flow:
  1) Business Profile setup (company name, email, phone, address, timezone)
  2) Upload logo
  3) Set base rates + minimum ticket + tax rate
  4) Configure service types + multipliers
  5) Configure add-on pricing
  6) Configure follow-up automations (Pro-only)
- Store everything per business so users can run multiple businesses in future.

FEATURES 1–6 (MUST IMPLEMENT)

FEATURE 1: Integrated Scheduling & Reminders
- Add “Jobs” module that supports:
  - Create job from accepted quote OR manually create job
  - Calendar-style list view (day/week list, simple)
  - Set date/time, duration, job type, notes
  - Optional recurrence (weekly/biweekly/monthly)
- Reminders:
  - Automated customer reminder 24 hours before job via SMS/email (Pro-only)
  - Message must be logged with status tracking
- Add “Calendar Sync” v1:
  - At minimum export .ics event OR “Add to Apple Calendar” link from job details

FEATURE 2: Automated Follow-Ups & Client Communication
- After a quote is “sent”, schedule follow-ups:
  - 1 hour: “Any questions?”
  - 24 hours: “Just checking in”
  - 3 days: “Spots fill quickly”
  - 7 days: “Last call”
- Allow business owner to edit templates (with variables):
  - {first_name}, {quote_total}, {service_type}, {quote_link}, {booking_link}
- Allow user to choose SMS, Email, or both (Pro-only)
- Include unsubscribe/STOP compliance language for SMS where required (“Reply STOP to opt out.”)
- If quote becomes accepted/declined, cancel remaining follow-ups automatically.

FEATURE 3: Professional Proposals + Reporting Dashboard
- Generate a branded Quote PDF / Proposal PDF:
  - Business logo, name, contact
  - Customer name + property details
  - Good/Better/Best table (if using packages)
  - Add-ons line items
  - Subtotal/tax/total
  - Terms + expiration date
  - “Accept Quote” button/link (public quote link)
- Reporting dashboard:
  - Quotes sent, accepted, close rate
  - Revenue won (accepted totals) by week/month
  - Average quote value
  - Pipeline by status (draft/sent/accepted/declined/expired)
- These analytics should update in real-time from the quotes table.

FEATURE 4: Booking + Scheduling + Payments in One Flow
- Create a shareable “Public Quote Page” accessible via token:
  - Displays quote details + total
  - Buttons: Accept / Decline
  - If accepted:
    - Prompt to choose appointment date/time from available slots (simple v1: user-defined availability windows OR manual confirm)
    - Option to collect deposit via Stripe Payment Link or Checkout (Pro-only)
- Payment:
  - Pro tier can enable deposits (either fixed $ or %)
  - Store payment status on quote/job
  - On successful payment, mark deposit_paid = true and log in communications

FEATURE 5: Replace Spreadsheets (Ops Tools)
- Add Customer/Job History:
  - For each customer: show all quotes + jobs + payments + communications timeline
- Add simple “Recurring Job Manager”:
  - See all recurring customers
  - Next scheduled date
  - Quick edit frequency or pause
- Add Checklists v1 (lightweight):
  - Allow business to define a default checklist per job type (touch_up, regular, deep)
  - On job details, show checklist with checkboxes (stored as job_checklist_items)
  - This is a major “spreadsheet replacement” feature

FEATURE 6: CRM-Style Lead Tracking + Notes
- Add CRM module:
  - Customer list with search, tags, status (lead/active/inactive)
  - Customer detail page with notes, timeline, next job, last quote
  - Pipeline view: Leads needing follow-up
- Add “Lead Source” field and simple filters
- Add “Tasks” v1:
  - Create follow-up task with due date (call/text)
  - Mark complete
  - Show “Today” tasks list

MONETIZATION: FREE VS PRO (MUST ADD)
Implement feature gating:
FREE:
- Create quotes
- Basic pricing settings
- Generate PDF
- Manual send quote (SMS/email manual but tracked)
- Basic dashboard (quotes count, avg quote)

PRO (Subscription):
- Automated follow-ups
- Job reminders
- Public quote acceptance page
- Scheduling + recurring jobs
- Deposits via Stripe
- CRM tasks + pipeline views
- Advanced reporting dashboard

Add a “Pricing / Upgrade” screen:
- Show Pro features list
- Monthly price (configurable constant)
- Integrate Stripe subscription flow (in-app purchase alternative is OK if easier; but if using IAP, implement with RevenueCat. Choose the simplest reliable path and implement.)

IMPORTANT: If you use Stripe subscriptions outside IAP, implement as a web-based checkout flow opened inside the app and store subscription status in backend. Make sure it’s App Store compliant (offer IAP if required). If uncertain, implement RevenueCat + Apple IAP for subscriptions.

UI REQUIREMENTS
- Match existing dark UI style from screenshots:
  - Dark background, rounded cards, blue accent, clean typography
- Bottom tab nav: Home, Pricing, Quotes, Jobs, Customers/CRM, Settings (you can merge Pricing into Settings if needed)
- Home dashboard should show:
  - Revenue this week
  - Pending quotes
  - Avg quote
  - Recent quotes
  - Upcoming jobs (new)

QUOTE FLOW REQUIREMENTS
- Create Quote:
  - Choose or create customer
  - Input property details
  - Select frequency
  - Select packages (Good/Better/Best) OR direct service type
  - Add add-ons
  - Show total and margin breakdown (margin can be stubbed if not requested)
- Send Quote:
  - Choose channel SMS/email
  - Customize message
  - Include quote link
  - Generate PDF and attach for email
- Quote status updates:
  - accepted/declined from public page
  - expired after expiration date
  - accepted triggers “Create Job” prompt

BACKGROUND JOBS / SCHEDULING
- Implement server-side scheduled jobs (cron):
  - Every 5 minutes check pending communications scheduled_for <= now and send
  - Every hour expire quotes past expires_at
  - Daily generate upcoming reminders
- Must be reliable and idempotent (no duplicate sends).

SECURITY + PRIVACY
- Row-level security so each business sees only their data
- Do not expose private info in public quote token pages beyond quote details
- Add privacy settings and explain SMS consent responsibility
- Handle opt-out:
  - If a customer replies STOP, mark customer.sms_opt_out = true and stop future SMS sends
  - Log inbound messages via webhook if possible; if too complex, implement opt-out list manually v1 (toggle on customer profile)

SETTINGS
- Business profile fields + logo upload
- Messaging settings (Twilio/Email sender) — these can be internal config if you don’t want user to enter API keys
- Templates editor (Pro):
  - Follow-up templates
  - Job reminder templates
- Automations toggles

TESTING / QUALITY
- Add basic unit tests for pricing calculations
- Add integration test notes (manual test checklist):
  - Create quote -> send -> follow-ups -> accept -> schedule job -> reminder -> deposit
- Provide a seed/demo mode for test customers and quotes

DELIVERABLES
1) Updated app with features 1–6 implemented
2) Clear file structure with comments
3) Environment variables documented for Supabase/Firebase + Twilio + email + Stripe
4) Step-by-step run instructions in README
5) A short list of “v2 enhancements” but ensure all v1 requirements above are done first

DO NOT
- Do not overcomplicate UI
- Do not add unrelated features
- Do not skip background job reliability
- Do not leave “TODO” placeholders for core flows

START NOW
First, analyze the existing project structure. Then implement the backend schema and auth. Then build the UI screens and flows. Then implement messaging/payment integrations. Then implement automations and reporting. Then do final cleanup and testing checklist.
