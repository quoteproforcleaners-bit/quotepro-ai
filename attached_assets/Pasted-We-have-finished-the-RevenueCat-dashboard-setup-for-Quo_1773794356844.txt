We have finished the RevenueCat dashboard setup for QuotePro iOS subscriptions.

Now I need you to wire RevenueCat into my iOS app and enforce plan access correctly.

Important:
This is for iOS only in this pass.
Do not work on web subscriptions.
Do not break existing auth, onboarding, dashboard, or quoting flows.
Before coding, first inspect the current app structure and identify:
- where app initialization happens
- where auth/login happens
- where subscription/paywall code already exists, if any
- where feature gating should live
- whether there is already a user context / auth context / subscription context
- where to store current plan tier in app state

RevenueCat setup already completed:
- Products:
  - quotepro_ai_monthly
  - com.quotepro.growth.monthly
  - com.quotepro.growth.annual
  - com.quotepro.pro.monthly
  - com.quotepro.pro.annual
- Entitlements:
  - starter
  - growth
  - pro
- Offering:
  - default
- Packages:
  - starter_monthly
  - $rc_monthly = Growth Monthly
  - $rc_annual = Growth Annual
  - pro_monthly
  - pro_annual

Goals:
1. Initialize RevenueCat correctly on iOS
2. Log users into RevenueCat after auth
3. Fetch offerings and render paywall options
4. Detect active entitlement and map user to:
   - starter
   - growth
   - pro
   - free
5. Enforce feature gating across the app

Implementation requirements:

1. SDK setup
- Configure react-native-purchases using EXPO_PUBLIC_REVENUECAT_API_KEY
- Only initialize for iOS
- Use the existing app root / provider structure cleanly

2. User identity
- After successful login or signup, call Purchases.logIn(user.id as string)
- On logout, call Purchases.logOut()

3. Offerings / paywall
- Fetch offerings from RevenueCat
- Read current offering
- Map packages:
  - starter_monthly
  - $rc_monthly
  - $rc_annual
  - pro_monthly
  - pro_annual
- Build a clean paywall model from these

4. Entitlement handling
- On app launch and after purchase, call getCustomerInfo()
- If entitlement active:
  - pro => tier = pro
  - growth => tier = growth
  - starter => tier = starter
  - else free
- Store tier in app state / context

5. Restore purchases
- Add a Restore Purchases action if it does not exist
- After restore, refresh entitlements and update app state

6. Feature gating
Enforce these plan rules:

Starter:
- max 20 quotes per month
- basic quoting only
- no AI quote builder
- no smart upsells
- no advanced automations
- no advanced reporting

Growth:
- unlimited quotes
- AI quote builder
- smart upsells
- follow-up tools
- quote requests / lead capture
- CRM / core business features

Pro:
- everything in Growth
- advanced automation
- advanced reporting / revenue intelligence
- premium / future-facing features

If some premium features are not fully built, set up clean gates/placeholders for future support.

7. Upgrade prompts
- If Starter user hits quote limit or tries a Growth feature, show upgrade prompt
- Growth should be the recommended upgrade
- Keep prompts clean and premium, not spammy

8. Validation
After implementation, validate:
- app initializes RevenueCat on iOS
- user is logged into RevenueCat after auth
- paywall fetches all packages
- purchases map to correct entitlement
- restore works
- feature gating works
- Starter does not accidentally unlock everything
- existing flows do not break

Output:
When done, provide:
- summary of what was implemented
- files changed
- files created
- where subscription tier is stored
- how quote limits are enforced
- where upgrade prompts are shown
- any assumptions / TODOs

Before coding, first inspect the app architecture and summarize the implementation plan.