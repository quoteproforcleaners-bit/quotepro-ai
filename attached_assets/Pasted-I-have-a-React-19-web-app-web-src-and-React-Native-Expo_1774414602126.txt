I have a React 19 web app (web/src/) and React Native Expo 
mobile app (client/) sharing a Node.js + Express backend.
analyticsEvents table exists in shared/schema.ts.
I need production-grade funnel telemetry.

1. DEFINE 20 CORE TRACKING EVENTS
   Create: shared/analytics-events.ts
   
   Export a typed enum of all trackable events:
   
   // Acquisition
   ACCOUNT_CREATED = 'account_created'
   TRIAL_STARTED = 'trial_started'
   
   // Activation (first value moments)
   FIRST_QUOTE_CREATED = 'first_quote_created'
   FIRST_QUOTE_SENT = 'first_quote_sent'
   FIRST_QUOTE_VIEWED_BY_CUSTOMER = 'first_quote_viewed'
   FIRST_QUOTE_ACCEPTED = 'first_quote_accepted'
   FIRST_JOB_COMPLETED = 'first_job_completed'
   
   // Engagement
   AI_AGENT_OPENED = 'ai_agent_opened'
   AI_AGENT_MY_BUSINESS_USED = 'ai_agent_my_business'
   AI_AGENT_COACH_USED = 'ai_agent_coach'
   AI_FOLLOWUP_SENT = 'ai_followup_sent'
   AI_FOLLOWUP_CONVERTED = 'ai_followup_converted'
   CALCULATOR_USED = 'calculator_used'
   
   // Revenue
   UPGRADE_CLICKED = 'upgrade_clicked'
   UPGRADE_COMPLETED = 'upgrade_completed'
   PLAN_CHANGED = 'plan_changed'
   
   // Retention signals
   DAY_7_ACTIVE = 'day_7_active'
   DAY_14_ACTIVE = 'day_14_active'
   DAY_30_ACTIVE = 'day_30_active'
   
   // Churn signals  
   CANCEL_INITIATED = 'cancel_initiated'
   QUOTE_QUOTA_HIT = 'quote_quota_hit'

2. SERVER-SIDE TRACKING FUNCTION
   Create: server/analytics.ts
   
   export async function trackEvent(
     userId: string,
     event: AnalyticsEvent,
     properties?: Record<string, any>
   ): Promise<void>
   
   - Inserts into analyticsEvents table
   - Properties stored as JSONB
   - Never throws — wrapped in try/catch, 
     logs errors silently (never block the main request)
   - Deduplicate "first_" events: check if event 
     already exists for userId before inserting

   Add trackEvent() calls in these locations:
   - authRouter.ts: ACCOUNT_CREATED, TRIAL_STARTED on register
   - quotesRouter.ts: FIRST_QUOTE_CREATED on first POST /api/quotes
   - quotesRouter.ts: FIRST_QUOTE_SENT after send succeeds
   - publicRouter.ts: FIRST_QUOTE_VIEWED_BY_CUSTOMER on /q/:token load
   - publicRouter.ts: FIRST_QUOTE_ACCEPTED on accept POST
   - jobsRouter.ts: FIRST_JOB_COMPLETED on first job complete
   - aiRouter.ts: AI_AGENT_OPENED, AI_FOLLOWUP_SENT
   - integrationsRouter.ts: UPGRADE_COMPLETED on webhook success
   - subscriptions: UPGRADE_CLICKED, CANCEL_INITIATED

3. CLIENT-SIDE TRACKING (WEB)
   Create: web/src/lib/analytics.ts
   
   export function track(
     event: string, 
     properties?: Record<string, any>
   ): void
   
   - POSTs to POST /api/analytics/track
   - Queues events if offline, flushes on reconnect
   - Fires these events from web components:
     - AI_AGENT_OPENED: when AI Assistant tab opens
     - UPGRADE_CLICKED: when any upgrade CTA is clicked
     - CALCULATOR_USED: when calculator produces result
     - CANCEL_INITIATED: when cancel flow begins

4. CLIENT-SIDE TRACKING (MOBILE)
   Create: client/lib/analytics.ts
   
   Same interface as web version.
   Fire from:
   - App.tsx onboarding completion
   - QuoteCreateScreen on quote send
   - AIAssistantScreen on open
   - SubscriptionScreen on upgrade tap

5. ADMIN FUNNEL DASHBOARD
   Create: server/routers/adminRouter.ts additions
   
   GET /api/admin/funnel
   Returns conversion rates between each funnel step:
   {
     accountsCreated: number,
     trialStarted: number,        // % of accounts
     firstQuoteSent: number,      // % of trials
     firstQuoteAccepted: number,  // % sent
     upgraded: number,            // % accepted
     day30Active: number,         // % upgraded
     conversionRates: {
       trialToFirstQuote: string, // e.g. "67%"
       quoteToAccepted: string,
       acceptedToUpgrade: string,
       upgradeToRetention: string
     }
   }
   Requires: admin secret header X-Admin-Key
   
   GET /api/admin/funnel?days=30
   Same data filtered to last N days.
   
   GET /api/admin/drop-off
   Returns: for each funnel step, number of users 
   who reached that step but NOT the next one 
   (these are your intervention targets).

6. ACTIVITY-BASED CHURN SIGNALS
   Add to the daily cron in server/index.ts:
   
   Daily at 8am — check for churn signals:
   
   Signal 1: Trial user, no login in 7 days
   → trackEvent(userId, 'churn_risk_inactive_trial')
   → Send email: "Are you still there, [name]? 
      Your trial has X days left"
   
   Signal 2: Paid user, no quote sent in 14 days
   → trackEvent(userId, 'churn_risk_paid_inactive')
   → Send email: "We noticed you haven't sent a quote 
      in 2 weeks — is everything ok?"
   
   Signal 3: User viewed upgrade page but didn't upgrade 
   (track upgrade_clicked without upgrade_completed 
   within 48 hours)
   → Send email: "You were this close — 
      any questions about upgrading?"

Show every new file, every trackEvent call location,
and the complete admin funnel endpoint response shape.