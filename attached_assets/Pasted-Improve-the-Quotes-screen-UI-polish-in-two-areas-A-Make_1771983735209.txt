Improve the Quotes screen UI polish in two areas:
A) Make the “Type” filter (All / Residential / Commercial) feel premium, high-contrast, and clearly interactive (not weak/disabled).
B) Make Commercial quotes visually distinct everywhere with a premium “PRO” badge + subtle card styling differences.
A) Type Filter Redesign (All / Residential / Commercial)
Increase separation from the background:
Wrap the entire filter row in a floating “glass” container with:
slightly lighter surface color than the screen background
1px border (white at ~8–12% opacity)
soft shadow (low opacity, larger blur)
optional blur if supported (glassmorphism)
Add 10–12px vertical padding around the segmented control so it feels like a component, not text on a bar.
Improve inactive state readability (it’s currently too dim):
Inactive labels should be medium contrast (not washed out).
Use consistent icon opacity with the label.
Add subtle “press” feedback (opacity shift + tiny scale down).
Make the active pill feel more premium:
Active pill should have a richer gradient (brand blue) + subtle inner highlight:
add a faint top highlight line inside the pill (white at ~10% opacity)
add slight glow outside the pill (blue shadow, very subtle)
Increase pill height slightly and add more horizontal padding.
Animate selection with a spring slide animation (indicator moves smoothly), plus:
active label scales to 1.02–1.04 briefly on selection
haptic feedback on selection
Add a micro “selected” indicator beyond color:
Add a tiny dot or underline INSIDE the active pill (very subtle) OR a small “spark” accent behind the icon.
Ensure it still looks clean and not gamified.
Typography:
Active tab text should be semibold.
Inactive should be regular/medium.
Ensure icons align vertically with text baseline and feel centered.
B) Make Commercial Quotes Visually Distinct (Premium Badge + Card Treatment)
Apply these changes to quote list items and quote details:
Premium “PRO” badge for Commercial:
If quote.type === “Commercial” then show a badge at top-right of the card area:
text: “PRO”
small crown or spark icon (optional)
gradient fill (gold/amber style) with subtle glow
rounded pill shape, semibold text, small size (doesn’t overpower)
Keep existing “Commercial” tag if needed, but the PRO badge should be the eye-catcher.
Subtle premium card styling for Commercial quotes:
Commercial quote cards should have:
a slightly different border (blue or gold tint at low opacity)
a faint corner accent (top-right) or a subtle diagonal sheen overlay
optional left-edge accent line (2–3px) in brand blue (low opacity)
Residential cards remain the default styling.
Commercial quote list row “signature” detail:
Add a small secondary line under sqft/beds that only appears for Commercial:
“Commercial Proposal” or “Proposal Ready”
Or show an icon next to sqft line (building icon) to reinforce type.
Keep it tasteful:
The premium effect must be subtle, not neon.
Commercial should feel “enterprise / proposal / premium,” not “flashy.”
Consistency:
The same PRO badge and subtle styling should appear:
on the quotes list cards
on the quote detail header
on the generated proposal preview screen (if applicable)
Deliverables:
Update the UI components/styles for the Type filter and quote cards.
Keep the layout spacing consistent with the rest of the screen.
Make sure the component works on various screen sizes and doesn’t clip.
Keep animations smooth and lightweight.