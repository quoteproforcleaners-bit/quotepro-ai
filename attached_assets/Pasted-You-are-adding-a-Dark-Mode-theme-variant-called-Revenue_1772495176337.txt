You are adding a Dark Mode theme variant called “Revenue Weapon” for QuotePro. You MUST reuse the existing semantic color tokens from the current theme system (colorPrimary, colorUrgency, colorSuccess, colorCardBg, etc.). Do NOT introduce random new colors per widget. The goal is a premium executive dark mode with strong hierarchy and high readability, optimized for TikTok screen recordings.

SCOPE
- Implement a dark theme configuration using the same semantic tokens.
- Add a user setting toggle: Appearance -> Light / Dark / System.
- Default remains System.
- Ensure all Home dashboard cards (Revenue Leak Detector, Sales Momentum, AI Engine, Streak) render correctly in dark mode.
- iPhone + iPad must be centered and readable. No overflow.

DESIGN GOALS
- Premium, “executive cockpit” look.
- Clean Apple-native, NOT gamer neon.
- Orange is sacred: ONLY revenue risk.
- Green is sacred: ONLY success / Engine ON.
- Blue remains primary (links + standard CTAs).
- Backgrounds are deep neutral; cards slightly lighter than page bg.

IMPLEMENTATION DETAILS
1) THEME TOKENS (Dark)
Create a dark theme map that populates ALL of these tokens:
- colorPageBg: very dark neutral (near-black, not pure black)
- colorCardBg: slightly lighter neutral than page bg
- colorDivider: subtle neutral line
- colorTextPrimary: near-white
- colorTextSecondary: muted gray
- shadowCard: reduce shadow; use subtle elevation or border instead (shadows look muddy on dark)
- radiusCard: keep consistent with light mode
- colorPrimary: brand blue (slightly brighter in dark mode for contrast)
- colorUrgency: orange (slightly softened to avoid neon)
- colorUrgencyBg: ultra subtle orange tint (use sparingly, prefer accent borders)
- colorSuccess: green (slightly softened)
- colorSuccessBg: ultra subtle green tint (rare)
- colorDanger: reserved for severe states only

2) COMPONENT RULES (Dark)
- Cards: use colorCardBg with subtle border (colorDivider) instead of heavy shadow.
- Headlines: textPrimary
- Subtext: textSecondary
- Numbers: textPrimary, except the “$ at risk” which uses colorUrgency for the number only.
- Buttons:
  - Primary CTA: colorPrimary background, white text
  - Urgency CTA: colorUrgency background, near-white text
  - Text links: colorPrimary

3) REVENUE LEAK DETECTOR (Dark Mode styling)
- Keep neutral card background (no orange wash).
- Add a thin top accent line or left border that reflects risk state:
  - Cooling: muted amber
  - Cold: orange
  - Critical: orange + small danger dot (do NOT turn whole card red)
- Ensure the “discomfort line” is readable but secondary.

4) SETTINGS TOGGLE
- Add Appearance setting with options: System / Light / Dark.
- Persist preference (same storage approach as other settings).
- Theme manager should:
  - If System: follow OS
  - Else: force chosen mode

5) TESTING / ACCEPTANCE
- Verify these screens in dark mode:
  - Home
  - Quote list/detail
  - Follow-up queue
  - Settings (Appearance)
- Ensure no hardcoded light colors remain on text/icons that become unreadable.
- Ensure iPad layout remains centered with max content width.

DELIVERABLES
- Dark theme token map implemented
- Appearance toggle in Settings implemented and persisted
- Home screen verified in dark mode with strong hierarchy and “Revenue Weapon” vibe
- No regressions in light mode

Proceed to implement now.