You are an expert iOS-first product designer + React Native (Expo) engineer. I need you to refactor my app’s color system to be more cohesive and premium while keeping the overall dark navy + electric blue look. The app currently uses a dark navy background, bright blue CTAs/selection states, and green in a “Revenue Potential” card plus a green “Premium” badge. I want a strict, intentional system:

GOAL
1) Make the UI feel like a premium SaaS product (Stripe/Linear vibe) with subtle depth.
2) Make BLUE the only brand/primary UI color.
3) Reserve GREEN strictly for “money/revenue/success” contexts (Revenue Potential, positive $ metrics, success states). Do NOT use green for “Premium” badges anymore.
4) Add a very subtle background gradient across screens for depth (top slightly darker, bottom slightly lighter). No loud gradients.

DELIVERABLES
A) Create a centralized design token file (colors, spacing, radius, typography) and refactor the app to use it everywhere instead of hardcoded hex values.
B) Update these specific screens/components to match the new system:
   - Quote Options screen (Good/Better/Best cards + Revenue Potential card + Add-ons list + primary CTA)
   - Auto Follow-ups screen (cadence cards + message tone cards + primary CTA)
C) Ensure accessibility: text contrast must be readable on dark backgrounds. Add comments where needed.

COLOR SYSTEM (use EXACT hexes below)
Background:
- bg0 (top): #0B1220
- bg1 (bottom): #111C2E
- surface0: #0F1A2A (cards)
- surface1: #121F33 (raised cards/selected containers)
- border: #22314A
- divider: #1B2940

Text:
- textPrimary: #F3F6FF
- textSecondary: #A9B4C7
- textMuted: #7E8AA3

Brand (Blue):
- brandPrimary: #2F7BFF (primary buttons, selected rings)
- brandHover: #2467DE
- brandSoft: rgba(47, 123, 255, 0.16) (subtle selected backgrounds)
- brandText: #DCE9FF

Revenue/Success (Green) ONLY for money/success:
- successPrimary: #16A34A (use ONLY for revenue numbers, “Revenue Potential” heading/icon, positive deltas)
- successSoft: rgba(22, 163, 74, 0.18) (Revenue Potential card background tint)
- successBorder: rgba(22, 163, 74, 0.35)

Warning / Error (optional if needed):
- warning: #F59E0B
- error: #EF4444

BADGE RULES
- “Premium” badge must NOT be green anymore. Use a neutral slate badge:
  - badgeBg: #1B2940
  - badgeText: #D7DEEA
  - badgeBorder: #2A3B57
- “Recommended” and “Best” badges should use BLUE (brandSoft bg + brandPrimary border + brandText text), not additional colors.

BACKGROUND GRADIENT
Implement a reusable <ScreenBackground> wrapper that renders a vertical gradient from bg0 to bg1 for every main screen (including the two screens above). If you can’t use a gradient library, simulate with two layered Views (but prefer expo-linear-gradient).

COMPONENT STYLING RULES
1) Cards: use surface0 with 16px radius, border 1px border color. Selected card gets border brandPrimary and background surface1 + optional brandSoft overlay.
2) Primary CTA button: brandPrimary background, white text, 14–16px radius, subtle shadow (very subtle).
3) Secondary text should use textSecondary, not lower-opacity white.
4) Remove any random teal/green accents outside revenue/success contexts.

IMPLEMENTATION DETAILS
- Search the codebase for hardcoded color hex strings and replace with tokens.
- Add a /src/theme/ directory with:
  - colors.ts
  - spacing.ts (8pt grid)
  - radius.ts
  - typography.ts (font sizes + weights)
  - index.ts export
- Update existing components to import from theme tokens.
- If I’m already using a ThemeProvider or styled-components, integrate tokens into that. If not, keep it simple with exported constants.

OUTPUT REQUIREMENTS
1) Provide the exact files you changed/created with code blocks.
2) Keep changes minimal but consistent across the app.
3) Do not break existing navigation or logic. This is a visual refactor only.
4) After implementation, list the before/after color usage summary and confirm green is only used for revenue/success.

Start by locating the current styling approach (StyleSheet, styled-components, Tailwind, etc.) and then implement the refactor accordingly.