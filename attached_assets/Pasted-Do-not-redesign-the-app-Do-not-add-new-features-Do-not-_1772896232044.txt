Do not redesign the app. Do not add new features. Do not change branding, layout, or styling unless absolutely necessary to fix a monetization bug.

This is a React Native mobile app with RevenueCat handling subscriptions and Apple App Store billing.

Your job is to perform a full technical audit of the current monetization flow and identify exactly why the app has installs and engagement but $0 revenue.

I want a diagnosis first, not random code changes.

AUDIT SCOPE

1) RevenueCat integration audit
Inspect the full RevenueCat implementation in the React Native codebase.

Verify all of the following:
- RevenueCat SDK is installed correctly
- RevenueCat is initialized correctly on app launch
- API key is correct for iOS
- The app user ID / anonymous ID flow is functioning correctly
- Offerings are fetched successfully
- The current offering exists and is not null
- Packages are loading correctly
- Product identifiers in code match RevenueCat and App Store Connect exactly
- Entitlements are correctly configured and mapped
- Active entitlement state is read correctly throughout the app
- Restore purchases is implemented correctly
- Customer info refresh logic is correct
- Sandbox vs production behavior is handled correctly
- Any RevenueCat errors are logged and surfaced clearly

Specifically inspect:
- app startup / initialization
- purchase package calls
- restore purchases calls
- customer info fetching
- entitlement checks
- subscription state persistence after app restart

2) Apple subscription / product audit
Audit the app-side assumptions about Apple subscriptions and flag likely configuration mismatches.

Verify or flag:
- product IDs expected by the app
- entitlement names expected by the app
- trial / introductory offer assumptions in UI text
- whether the code expects one subscription but RevenueCat is configured differently
- whether paywall copy matches the real product setup

If direct Apple config cannot be verified from code alone, create a checklist of exact items I need to confirm in App Store Connect and RevenueCat.

3) Paywall trigger audit
Map every point in the app where a paywall should appear.

For each paywall trigger, verify:
- what action causes it
- whether it actually appears
- whether users can bypass it
- whether premium features can run without subscription checks
- whether free users are getting too much value before seeing the paywall
- whether the paywall is shown at the right moment in the user journey

Create a list of:
- all premium features
- all free features
- all upgrade entry points
- all missing or weak paywall triggers

4) Feature gating audit
Inspect all premium feature checks in the React Native code.

Verify:
- premium screens are protected
- premium buttons/actions are protected
- background logic or API calls are protected
- AI features are protected if intended to be paid
- quote generation limits work if intended
- follow-up / CRM / proposal / automation features are correctly gated
- no premium functionality is accidentally available to free users
- entitlement checks are consistent across the app

Identify any places where:
- UI is locked but backend logic still runs
- UI appears unlocked before subscription state loads
- a race condition allows temporary premium access
- local state incorrectly marks users as pro
- cached values override live subscription state

5) Purchase flow audit
Test and inspect the full purchase journey in code.

Verify:
- tapping upgrade launches the correct RevenueCat purchase flow
- package selection is correct
- errors are handled visibly
- cancellations are handled cleanly
- successful purchase updates app state immediately
- purchase success unlocks premium features without requiring restart
- restore purchases works properly for returning users
- expired or inactive users lose premium access correctly
- loading states prevent double-taps or broken UX

Flag any likely blockers to conversion such as:
- broken purchase button
- offering not loaded
- paywall rendered without package data
- purchase success not updating entitlement state
- restore flow hidden or broken
- confusing plan naming
- bad error handling

6) Trial / intro offer audit
Audit all trial messaging and logic.

Verify:
- any “free trial” language is real and matches product config
- intro offers are read from actual product data if possible
- no misleading text exists
- the app clearly explains whether the user is charged immediately or after trial
- trial eligibility assumptions are not hardcoded incorrectly

If the app cannot reliably know eligibility, flag any copy that should be softened.

7) Onboarding-to-conversion audit
Review the user journey from first open to potential upgrade.

Assess:
- how fast the user reaches core value
- whether the app demonstrates enough value before asking for money
- whether premium benefits are clearly explained
- whether upgrade prompts happen after useful actions
- whether onboarding explains what free includes vs paid includes
- whether the user can fully use the app without ever understanding why to upgrade

Identify:
- missing upgrade moments
- weak value communication
- confusing free/paid boundaries
- parts of the journey where monetization intent disappears

8) Analytics / event tracking audit
Inspect existing analytics for monetization events.

Verify whether the app tracks:
- paywall viewed
- upgrade tapped
- purchase started
- purchase completed
- purchase failed
- restore started
- restore completed
- trial started
- premium feature blocked
- premium CTA tapped

If tracking is missing, propose lightweight instrumentation only.
Do not add heavy analytics tools unless they already exist in the app.

9) React Native technical audit
Inspect for React Native-specific issues that could break monetization.

Check for:
- initialization timing problems
- async state bugs
- stale hooks / useEffect dependency issues
- incorrect context/provider usage for subscription state
- navigation guards not waiting for subscription data
- inconsistent state across screens
- purchase flow bugs caused by unmounted components
- app state / foreground refresh issues
- iPhone vs iPad logic inconsistencies if present

10) Logging audit
Add or improve lightweight logs only if needed to diagnose.

I want logging around:
- RevenueCat initialization
- offerings fetch
- current offering/package availability
- purchase attempt
- purchase success/failure/cancel
- customer info refresh
- entitlement state result
- restore purchases result
- paywall display events
- premium feature block events

Keep logs clean, developer-friendly, and easy to remove later.

DELIVERABLES

Phase 1: Audit only
Do not make broad code changes yet.

Provide a clear audit report with these sections:

A. Executive summary
- what is working
- what is broken
- what is risky
- most likely reasons revenue is still $0

B. RevenueCat integration findings
C. Paywall / purchase flow findings
D. Feature gating findings
E. Trial / messaging findings
F. Analytics findings
G. Top 5 highest-priority issues

Phase 2: Fix plan
After the audit, provide a prioritized implementation plan:

- Critical fixes
- Important fixes
- Nice-to-have conversion improvements

For each item include:
- issue
- impact
- recommended fix
- code area affected
- risk level

Phase 3: Ask for approval
After the audit and fix plan are complete, stop and ask me for approval before changing code.

IMPORTANT CONSTRAINTS
- No redesign
- No unrelated refactors
- No new features unrelated to monetization
- No changing random copy across the app unless directly tied to conversion clarity
- No assumptions without evidence from code
- If something cannot be verified from code, explicitly say so and create a checklist for me to validate manually

GOAL
Determine exactly why the React Native app is getting installs and engagement but no paid conversions, and give me the clearest path to fixing the current RevenueCat + paywall + subscription flow.