You are a senior React Native (Expo) engineer working inside my existing iPhone app “QuotePro” (built in Replit). Convert the current Home/Dashboard screen into an “AI Command Center” WITHOUT breaking existing navigation or features. Do NOT create a new app. Minimal refactors. Reuse existing components/styles.

GOAL
Replace the current “dashboard-first” Home tab with an AI-first home experience:
1) A large command input box (“Ask QuotePro…”) that accepts natural language.
2) Rotating example prompts that animate in/out (“Create a deep clean quote for John…”, etc.).
3) Quick Action chips for common tasks.
4) A lightweight “Today at a glance” section (3 small stat cards max).
5) A command router that parses the user’s intent and routes them into the right screen with prefilled data when possible.
6) AI is not fully live yet: implement with a local deterministic stub that returns helpful responses and actions. Add a feature flag to later swap in real AI API.

FEATURE FLAGS
Create a simple config:
- aiHomeEnabled: true (default true)
- aiEnabled: false (default false)  // same as earlier if it exists
- aiLaunchEtaDays: 14 (default 14)
If aiEnabled is false, show a banner: “AI features launch in ~1–2 weeks” and an Upgrade CTA (placeholder).

UX SPEC (HOME TAB)
Top area:
- Header: QuotePro + greeting (“Good evening, Two Maids of Malvern” can remain but make it smaller)
- Primary element: a big rounded “command input” card
  - Title: “What would you like to do?”
  - TextInput placeholder: “Ask QuotePro… (e.g., ‘Create a deep clean quote for John’)”
  - Send button (paper plane icon) and optional microphone icon (nonfunctional placeholder ok)

Below input:
- Animated rotating example prompts (1 at a time, crossfade/slide every 4 seconds).
  - Tapping an example fills the input.
  - Include 12 examples; randomly rotate; never repeat the same twice in a row.

Below examples:
- Quick action chips (horizontal scroll):
  - “New Quote”
  - “Follow up quotes”
  - “This month booked”
  - “Draft a reply”
  - “Unpaid invoices” (if invoices don’t exist, show “Coming soon”)
  - “Schedule a job”
Tapping chips should either route directly or prefill input and run.

Below chips:
- “Today at a glance” section (max 3 compact cards):
  - Quotes needing follow-up (computed from quote status/sentAt/lastContactAt if available; otherwise show 0)
  - Jobs today (count by date if jobs exist)
  - This month revenue (sum accepted/won quotes OR jobs if you track)
Keep the design minimal, less “dashboardy” than current.

COMMAND ROUTER / INTENTS
Implement a lightweight on-device intent parser (no AI dependency) that supports these intents:
1) CREATE_QUOTE: “create quote”, “new quote”, “deep clean quote for John”, “quote for 3 bed 2 bath…”
   - Action: navigate to the New Quote flow and prefill customer name (if provided) and service type if detected (“deep clean”, “move out”, “recurring”, etc.).
2) METRICS_THIS_MONTH: “how many cleans booked this month”, “revenue this month”, “close rate”
   - Action: show an in-app response card below the input summarizing the metric (no navigation required).
3) FOLLOW_UP_QUOTES: “follow up”, “unclosed quotes”, “missed revenue”
   - Action: navigate to Revenue tab (or Quotes list filtered) and highlight follow-up section.
4) DRAFT_REPLY: “write a text”, “respond to customer”, “too expensive”, “handle objection”
   - Action: navigate to AI Assistant screen if it exists; otherwise open a new screen/modal “AI Draft” that returns stubbed suggestions and provides Copy / Open SMS / Open Email.
5) OPEN_CUSTOMER: “open customer John”, “show customer Sarah”
   - Action: navigate to Customers and preselect if match exists.
6) OPEN_QUOTE: “open quote for John”, “latest quote”
   - Action: navigate to quote details if found.

If the intent is unknown:
- Show a helpful response:
  “I can help with: creating quotes, follow-ups, metrics, drafting replies. Try one of the examples below.”

AI STUB (DETERMINISTIC)
Create a local function:
- `runAiCommand(commandText, appData) => { intent, responseText, navigation?: { tab, screen, params }, suggestedActions?: string[] }`
This is NOT real AI. Use simple keyword/regex rules + deterministic templates.
Return concise, helpful responses.

EXAMPLE PROMPTS (ROTATING)
Include prompts like:
- “Create a deep clean quote for John Q Public”
- “How many cleans have I booked this month?”
- “Show quotes I haven’t followed up on”
- “Draft a text: ‘Thanks for reaching out — here’s your quote’”
- “What’s my close rate this month?”
- “Create a biweekly quote for a 3 bed 2 bath”
- “Customer says I’m too expensive — draft a reply”
- “Who are my top customers by revenue?”
- “Schedule a job for tomorrow at 10am”
- “Add an add-on for inside oven to my last quote”
- “Show me jobs scheduled this week”
- “How much revenue did I do this week?”

ANIMATIONS
- Use React Native Animated (built-in) or Reanimated ONLY if already installed.
- Simple fade/slide is fine; keep performance smooth.

INTEGRATION REQUIREMENTS
- Identify where the Home screen is defined and replace its content with this new layout.
- Reuse existing typography/colors/cards/buttons if present.
- Ensure navigation works on iOS.
- Do not remove existing tabs (Customers, Quotes, Jobs, Revenue, Settings).
- Keep “Recent Quotes” accessible: add a small “Recent Quotes” button/link that navigates to Quotes tab.

DATA ACCESS
- Use existing data sources/state (quotes/customers/jobs).
- If you don’t have status/sentAt fields, gracefully degrade and don’t crash (assume 0).

FILES / DELIVERABLES
1) Implement AI Command Center on Home screen.
2) Add command router + stub.
3) Add rotating examples component.
4) Add quick action chips.
5) Add minimal “Today at a glance” section.
6) Print a short summary of files changed + how to extend the intent router.
7) Ensure the app compiles.

PROCESS
Before coding:
- Scan the repo structure and locate:
  - Home screen component
  - Navigation config
  - Data store for quotes/customers/jobs
  - Existing UI components
Then:
- List the exact files you will modify/add
- Implement feature with minimal risk
- Keep code clean and commented

Start now.
