Fix RevenueCat production/TestFlight subscription loading failure.

Problem:
On TestFlight (iPad Air 11-inch, iPadOS 26.3), tapping Subscribe shows:
“Not Available – The subscription offering couldn't be loaded.”

Goal:
1) Ensure RevenueCat initializes correctly in RELEASE builds.
2) Ensure offerings are fetched reliably before purchase.
3) Never show a blocking modal just because offerings fail to load.
4) Add lightweight diagnostics so we can see if offerings/packages are actually loading.
5) Use offerings.current instead of hardcoded identifiers.

DO NOT change product IDs or pricing.
DO NOT change business logic.
ONLY fix initialization, offering loading, error handling, and UI state.

---

STEP 1: Ensure RevenueCat initializes exactly once at app startup

• Locate where Purchases.configure() is called.
• Move it to app root (App.tsx / entry point).
• Ensure it runs exactly once.
• Ensure the iOS Public SDK key is hardcoded or securely embedded in the release build (not missing via env var).
• Add console log:
  console.log("RevenueCat configured with key:", key.slice(0,8));

---

STEP 2: Create a robust offerings loader hook/service

Create a reusable hook: useRevenueCat()

State:
- isConfigured
- offeringsStatus: 'loading' | 'ready' | 'error'
- packages: []
- errorMessage

Logic:
1) On mount:
   - await Purchases.getOfferings()
   - use offerings.current (NOT offerings["something"])
   - if current and availablePackages.length > 0:
        set packages
        set status 'ready'
     else:
        set status 'error'
2) Retry up to 2 times with small delay (500ms, 1200ms)
3) Log:
   console.log("Offerings loaded:", offerings)
   console.log("Packages count:", packages.length)

---

STEP 3: Update Paywall screen behavior

Rules:
• When offeringsStatus === 'loading'
    Show: "Loading subscription options..."
    Disable Subscribe button

• When offeringsStatus === 'error'
    Show inline error card (NOT modal):
        Title: "Subscriptions temporarily unavailable"
        Buttons: [Retry] [Continue Free]
    Keep Restore Purchases visible

• When offeringsStatus === 'ready'
    Enable Subscribe button

---

STEP 4: Fix Subscribe button handler

Before attempting purchase:
• if offeringsStatus !== 'ready' -> return
• if no selected package -> auto select first package

During purchase:
• Set purchasing state true
• Disable button to prevent double tap
• On success:
    await Purchases.getCustomerInfo()
    Unlock premium
• On user cancel:
    Show small toast "Purchase cancelled"
• On error:
    Map error to friendly message
    DO NOT show generic modal

---

STEP 5: Ensure Restore works independently

Restore button:
• Call Purchases.restorePurchases()
• If entitlement active:
    Unlock premium
• If none:
    Show: "No active subscription found."

Restore must work even if offerings failed.

---

STEP 6: Add lightweight diagnostics to paywall (hidden)

Add a small text at bottom (visible only in __DEV__ or TestFlight):
Show:
- isConfigured
- offeringsStatus
- packages.length

Example:
RevenueCat: configured
Offerings: ready
Packages: 1

---

STEP 7: Verify on iPad

Confirm:
• Paywall loads without error modal
• Subscribe is disabled until packages loaded
• Offerings.current is used
• No blocking "offering couldn't be loaded" modal

Deliver:
• New useRevenueCat hook/service
• Updated paywall with loading/ready/error states
• No hardcoded offering identifiers
• Clean error handling
• Diagnostics logs added