Important safety instruction:
Do not overwrite or break my existing app. This must integrate cleanly into what I already built.
Preserve my existing auth, CRM, quote builder, intake flow, navigation, customer records, job records, and current styling unless changes are explicitly required below.
Before making changes, first inspect the codebase and identify the exact integration points, then implement carefully.
If there is any conflict, preserve the current app and modify only what is needed.

I need you to fix and improve the following issues in my existing app. These changes are HIGH PRIORITY because QuotePro’s main promise is fast quoting + revenue growth for cleaning business owners.

CORE PRODUCT PRINCIPLE:
This app is first and foremost a revenue tool.
The home screen, quote generation, upsells, and workflows should all be optimized around:
- creating quotes quickly
- increasing quote accuracy
- increasing close rates
- increasing revenue per lead
- encouraging better / higher-value options
- helping the user upsell intelligently

Please inspect the current codebase first and then implement the following:

--------------------------------------------------
1. FIX PRICING LOGIC IMMEDIATELY (TOP PRIORITY)
--------------------------------------------------

I am fairly certain the app is not following the intended pricing logic.
It quoted a deep clean at $120, which is unacceptable and will destroy trust.

Tasks:
- audit the current quote generation logic in detail
- inspect all pricing calculations, pricing tables, defaults, AI quote generation logic, fallback logic, and any deep clean pricing adjustments
- identify why a deep clean could be generated at an unrealistically low price
- fix the bug(s)
- make sure quotes respect the user’s configured pricing rules and logic
- ensure the AI quote builder does NOT bypass or ignore pricing rules
- ensure deep cleans, premium cleans, recurring cleans, add-ons, square footage logic, bedroom/bathroom logic, and any service-type multipliers are correctly applied
- if the system has fallback pricing, make the fallback safe and conservative rather than dangerously low

Add protections:
- implement guardrails to prevent clearly underpriced quotes
- if a generated quote falls below expected minimum pricing thresholds, flag it or prevent generation
- add a sanity check layer before final quote display/send
- add logging or debugging notes in code so pricing issues are easier to trace later

Important:
Pricing accuracy must take priority over speed.
Do not leave any path in the app where AI can generate a quote that ignores configured pricing logic.

Deliverable for this section:
- inspect the current pricing system
- fix the pricing bug
- explain in comments or summary what was wrong and how it was corrected

--------------------------------------------------
2. ADD “QUOTE REQUEST” TILE TO HOME SCREEN
--------------------------------------------------

On the Home screen, add a new quick action tile called:

- Quote Request

It should appear next to:
- Send Intake

This tile should connect to the public quote request / lead capture feature if that already exists.
If it does not fully exist yet, route it to the appropriate settings/setup page or placeholder action related to quote requests.

Use the current design system and card/tile style already in the app.

--------------------------------------------------
3. CREATE A SCALABLE SHORT URL STRATEGY
--------------------------------------------------

The current quote request URL is too long and ugly.
We need a better short URL strategy that can scale.

Please inspect the current implementation and improve it.

Goal:
Give each app user a short, clean, scalable public quote request link.

I want something much shorter and cleaner than a long generated URL.

Please design and implement the best practical MVP option for scale.

Good possible patterns:
- /q/{slug}
- /r/{slug}
- quotepro.ai/{slug} if routing allows
- getquotepro.ai/{slug} if routing allows
- a short code system if appropriate

Requirements:
- keep it unique per app user / business
- make it easy to copy and share
- maintain compatibility with the existing quote request workflow
- keep it scalable for large numbers of users
- use short slugs or short public identifiers when possible
- preserve backwards compatibility if old links already exist
- redirect old long links to the new shorter format if needed

Also:
- improve the settings UI so users see the shorter link
- add a prominent “Copy Link” button
- if useful, keep both a readable slug version and a short code version internally, but expose the cleaner one publicly

Do NOT use an external URL shortener dependency unless absolutely necessary.
I want a native product-level solution that scales.

--------------------------------------------------
4. ALWAYS SHOW GOOD / BETTER / BEST OPTIONS
--------------------------------------------------

Right now, if a customer requests a specific cleaning type in the intake form, the auto-generated quote only shows that one option.

That is not what we want.

Even if the customer selects a specific cleaning request, the app should still generate and present:
- Good
- Better
- Best

The selected service should influence the recommendation and pricing logic, but the quote should still provide multiple options so the app user can maximize revenue and close rate.

Tasks:
- inspect the existing quote generation flow
- update quote logic so every quote can include tiered options
- preserve the customer’s requested service as an anchor or recommended option if appropriate
- generate smart tiered pricing around the request
- make sure the UI clearly displays Good / Better / Best
- ensure the auto-generated quote and AI-assisted quote both support this behavior

For example:
- if customer asks for standard clean, still present:
  - Good = standard clean
  - Better = standard + add-on improvements or better frequency
  - Best = premium/deep/recurring/value-maximizing option
- if customer asks for deep clean, still present:
  - Good = base deep clean
  - Better = deep clean + add-ons or recurring follow-up
  - Best = highest-value recommended package

Do not make the upsells feel random.
They should be logical, revenue-oriented, and believable.

--------------------------------------------------
5. ADD SMART UPSELL RECOMMENDATIONS
--------------------------------------------------

We need to recommend smart upsells to the app user because the #1 focus of this app is to create quotes quickly and increase revenue quickly.

Tasks:
- inspect current quote flow and determine where upsell intelligence should live
- add smart upsell recommendations to the quote-building experience and/or AI-generated quote flow
- recommendations should be practical and tied to the customer’s intake details

Possible upsell examples:
- inside fridge
- inside oven
- baseboards
- pet hair treatment
- move-in / move-out extras
- recurring service discount framing
- deep clean first, then recurring maintenance
- extra bathroom / extra bedroom pricing adjustments
- laundry / dishes / interior windows if relevant to the current app model
- commercial add-ons if applicable

Important:
Upsells should not be random.
They should be based on:
- clean type
- property size
- home condition
- pets
- frequency
- customer notes
- commercial vs residential
- obvious value-building opportunities

Please implement:
- a smart recommendation engine or rules-based upsell suggestion layer
- UI that surfaces suggested upsells clearly to the app user
- ability for the user to quickly include/exclude recommended upsells
- if appropriate, use AI only as a helper, but pricing logic for upsells must still follow configured pricing rules

This should feel like:
“Here are the fastest, highest-likelihood ways to increase this quote value.”

--------------------------------------------------
6. REDESIGN QUICK ACTIONS ON THE FRONT DASHBOARD
--------------------------------------------------

The quick actions on the front dashboard should be all about revenue.

Replace the current quick actions with:
- New Quote
- Smart Intake
- Quote Requests

Remove:
- Customers
- View Jobs

Reason:
- the front dashboard should focus on revenue generation
- View Jobs is already in the bottom menu
- the dashboard should push the user toward quote creation, lead capture, and intake flow

Tasks:
- inspect the current dashboard quick action implementation
- replace/reorder the quick actions accordingly
- make sure the design remains polished and consistent with the current app
- route each action to the correct screen/flow

If “Smart Intake” currently exists under a different label, keep naming consistent and make it user-friendly.

--------------------------------------------------
7. IMPLEMENTATION NOTES / PRODUCT EXPECTATIONS
--------------------------------------------------

Please approach this like a senior product engineer working on a live SaaS app.

Requirements:
- inspect the current project structure before coding
- identify the existing quote logic, pricing logic, home dashboard, quick actions, public quote request routing, and settings pages
- reuse existing components, utilities, and styling wherever possible
- do not duplicate business logic unless absolutely necessary
- keep code production-ready
- make the changes tightly integrated with the existing product
- preserve current mobile responsiveness
- do not create disconnected prototype code
- ensure all new UI feels native to QuotePro

--------------------------------------------------
8. TESTING / VALIDATION
--------------------------------------------------

Please add or perform practical validation for the changes.

At minimum:
- verify deep clean pricing cannot produce absurdly low results
- verify Good / Better / Best appears even when a customer requested a specific service
- verify smart upsell suggestions appear appropriately
- verify the new Quote Request quick action appears on the home screen
- verify dashboard quick actions are now:
  - New Quote
  - Smart Intake
  - Quote Requests
- verify the shorter public URL works and points to the correct quote request flow
- verify old links still work if they existed before

If the project has a place for tests, add tests.
If not, at least add robust validation logic and document how the feature was validated.

--------------------------------------------------
9. OUTPUT / DELIVERABLES
--------------------------------------------------

When done, provide:
- a short summary of what you changed
- what caused the pricing issue
- what files were modified
- what new files were created
- any migration/setup steps needed
- any assumptions made
- any follow-up recommendations for making quote accuracy even stronger

Final instruction:
Do not treat this as a redesign project.
Treat this as a high-priority revenue and trust optimization sprint for QuotePro.
Pricing accuracy and revenue-focused quote generation are the most important outcomes.
Before coding, first inspect the current app architecture and summarize the implementation plan.