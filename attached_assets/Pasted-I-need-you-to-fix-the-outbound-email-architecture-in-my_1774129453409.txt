I need you to fix the outbound email architecture in my multi-tenant SaaS app, QuotePro.

Problem:
When a tenant tries to send an email from the software, the send fails with a sender verification/authentication error. Right now the app is behaving as if each tenant’s email address needs to be verified as a sender, which is the wrong architecture for a multi-tenant SaaS product.

Important context:
QuotePro is a multi-tenant SaaS app.
Each tenant is a different cleaning business.
Tenants need to be able to send quotes, follow-ups, confirmations, reminders, and other customer emails from inside the app.

New sending requirement:
All outbound emails should be sent from my central platform email account:
quote@getquotepro.ai

This is the sending address I want the system to use by default for all tenants.

I do NOT want the system trying to send directly from arbitrary tenant email addresses.
I do NOT want tenants to have to verify themselves as senders.
I do NOT want tenants seeing infrastructure-level sender-authentication errors.

Correct architecture to implement:
- Actual From Email: quote@getquotepro.ai
- From Name: use the tenant’s business name if available, otherwise a sensible default
- Reply-To: use the tenant’s business email that they entered in settings
- Delivery infrastructure: use the existing mail provider setup, but refactor the logic so all outbound mail is sent from quote@getquotepro.ai

Example:
From: Mike’s Cleaning <quote@getquotepro.ai>
Reply-To: owner@mikescleaning.com

This gives the tenant a branded experience while keeping sending centralized and low-friction.

What I need you to do:
1. Audit the current email-sending implementation
2. Identify exactly why the current sender verification/authentication error is happening
3. Refactor the email architecture so all outbound mail sends from quote@getquotepro.ai
4. Use tenant business email only as Reply-To, not as the actual From address
5. Update the UI and settings flow accordingly
6. Remove any logic that assumes each tenant must be verified as a sender
7. Make the experience simple and low-friction for tenants

What I want implemented:
1. Centralized outbound sending using quote@getquotepro.ai
2. Tenant settings for:
   - business name
   - business email / reply-to email
   - optional sender display name if needed
3. All email flows updated to use:
   - from email = quote@getquotepro.ai
   - from name = tenant business name or configured sender name
   - reply-to = tenant business email
4. Clean validation if tenant reply-to email is missing or invalid
5. Better user-facing messaging for send failures
6. Logging for outbound send attempts and failures

Please inspect and update all relevant outbound email flows, including:
- quote emails
- follow-up emails
- appointment confirmations
- reminders
- review asks
- any other customer-facing outbound emails sent from the app

Critical UX requirement:
Tenants should not see messages telling them to go verify a sender in SendGrid or any provider dashboard.
That is platform-infrastructure complexity and should not be exposed to end users.

Instead:
- if their business email is missing, prompt them to add a reply-to email in settings
- if sending fails, show a clean generic error and log the real technical reason internally

Please inspect the codebase for:
- where outbound emails are sent
- how the current from address is determined
- where the current sender-authentication error is generated
- where tenant/company email settings live
- how the mail provider is configured
- whether there is any code incorrectly trying to use tenant emails as senders

Implementation requirements:
1. Inspect the actual code and identify the root cause
2. Refactor outbound mail sending to always use quote@getquotepro.ai as the sender
3. Use tenant email only as Reply-To
4. Update settings/UI where needed
5. Replace the current broken sender-verification UX
6. Ensure this works across the whole product
7. Make the solution clean and extensible

Future-ready architecture:
Please build this so I can later add optional advanced sending modes such as:
- custom sending domain per tenant
- Gmail/Google Workspace connection
- Outlook/Microsoft 365 connection

But those are future enhancements only.
For now, the default and required sending model should be centralized sending from quote@getquotepro.ai.

Deliverables:
1. Explain the root cause in plain English
2. Explain the correct architecture for this multi-tenant setup
3. Implement the fix in the codebase
4. Show exact files/functions changed
5. Update any tenant settings screens if needed
6. Improve user-facing error handling
7. Explain how to test the full flow end to end

Success criteria:
- all outbound mail sends from quote@getquotepro.ai
- tenant business name appears as the sender name when appropriate
- replies go to the tenant’s real business email
- tenants do not need sender verification
- confusing infrastructure-level errors are removed from the tenant UI
- the app works cleanly in a multi-tenant environment

Important:
Do not stop at analysis or recommendations.
Inspect the real code, implement the architecture fix, and summarize exactly what changed.

Before coding, first explain:
1. why the current approach is broken
2. what the correct centralized sending model should be
3. how you will refactor the code
Then implement it.