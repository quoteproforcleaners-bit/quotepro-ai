You are an expert full-stack engineer working inside an existing Expo React Native app called QuotePro (dark UI, Home dashboard with a “Getting Started” checklist, and bottom tabs: Home, Customers, Quotes, Jobs, Settings). We are ADDING a new first-class menu option called “Social” that positions the feature as:

“Turn on your AI Sales Assistant”
Meaning: it auto-responds to IG/FB DMs that show buying intent and routes prospects through an instant quote funnel with revenue attribution.

PRIMARY FEATURE: AI DM Auto Quote Funnel + Revenue Attribution
- Connect Instagram + Facebook messaging (Meta)
- Receive inbound DMs via webhooks
- AI intent detection (“how much”, “quote”, “availability”, “service area”, etc.)
- Auto-reply with a Quick Quote link
- Web quote form generates instant quote using QuotePro pricing settings
- Create Lead + Quote records
- Attribute leads/quotes/revenue to channels (IG/FB; TikTok placeholder manual)
- Show dashboards + response time metrics

IMPORTANT PRODUCT NOTE
TikTok DM APIs are not reliably available to most public apps. In MVP:
- Build TikTok as a channel in the data model + dashboards
- Provide manual “Create TikTok Lead” workflow by pasting DM text
- Do NOT fake TikTok DM API integration.

CRITICAL: IN-APP ONBOARDING MUST BE BUILT (NOT JUST BACKEND)
We must build an onboarding flow that feels native to QuotePro and drives to a “magic moment” within 10 minutes:
“I sent a test DM and my AI Sales Assistant replied with a quote link.”

NAVIGATION INTEGRATION
Add “Social” as a first-class destination:
- Prefer adding a 6th bottom tab labeled “Social”.
- If tabs are constrained, add:
  (1) a prominent “Social” tile card on Home
  (2) a Settings menu item “Social / AI Sales Assistant”
Either way, Social must be easy to access.

SOCIAL ONBOARDING EXPERIENCE (REQUIRED)
When user opens Social for the first time (or if not fully set up), show a guided setup wizard:

Screen 0: Intro
Title: “Turn on your AI Sales Assistant”
Bullets:
- Auto-reply to pricing + availability DMs (IG/FB)
- Send instant quotes in seconds
- Track revenue from social
CTA: “Start Setup (5–7 minutes)”

Step 1: Pricing Check
- If PricingSettings not configured, show:
  “Set your pricing so we can generate instant quotes.”
  CTA: “Set Pricing”
  (deep link to existing pricing settings screen or create one)
- If configured, show “Pricing looks good ✅” and Continue

Step 2: Connect Channels
- Buttons: “Connect Instagram” and “Connect Facebook”
- After OAuth success show:
  “Instagram Connected ✅” “Facebook Connected ✅”
- Include reassurance copy:
  “We only auto-reply when someone asks about pricing/availability. We never broadcast or spam.”

Step 3: Turn On Automation
- Toggle: “AI Auto-Replies: ON/OFF”
- Slider: “Intent Sensitivity” (default 0.70)
- Optional quiet hours
- Explain with example DM + example reply

Step 4: Test It Now (Magic Moment)
- Prompt: “Open Instagram or Facebook and DM your business: ‘How much for a deep clean?’”
- In-app status:
  “Waiting for your test message…”
- When webhook receives inbound message for this user:
  - Show success:
    “Inbound DM received ✅”
    “Auto-reply sent ✅”
    “Quote link generated ✅”
  - Display the exact auto-reply that was sent
- If no webhook arrives in 2 minutes, show troubleshooting tips and allow “Simulate Test DM” in DEV/STUB mode.

Step 5: Done
- Show dashboard preview with metrics:
  Leads from Social, Quotes, Revenue, Avg response time
- CTA: “Go to Social Dashboard”

ALSO: HOME CHECKLIST INTEGRATION
On the Home “Getting Started” checklist, add a new section or items:
- “Set Pricing”
- “Connect Instagram/Facebook”
- “Enable AI Auto-Replies”
- “Run Test DM”
Show completion states and progress.

SOCIAL SECTION SCREENS (POST-ONBOARDING)
1) Social Dashboard
- Connection status cards (IG/FB/TikTok)
- KPI tiles: Leads, Quotes, Revenue, Avg response time (7d/30d toggle)
- Recent Conversations list (badge: Auto-replied / Opted-out / Ignored)

2) Connections Screen
- Connect/disconnect
- Show token expiry
- Permissions checklist
- Webhook status (last event received timestamp)

3) Automation Rules Screen
- Auto-replies toggle
- Intent threshold slider
- Quiet hours
- Reply templates editor (default short)
- Opt-out keywords list
- Manual Opt-Out list

4) Leads from Social Screen
- Leads list filtered by source channel
- Tap opens lead detail (reuse existing patterns)

5) Attribution Insights Screen
- Leads by channel, Quotes by channel, Revenue by channel
- Close rate by channel (Won / Total quoted)
- Response time by channel
(MVP UI can be simple tables; charts optional)

6) Create TikTok Lead (Manual)
- Paste DM text
- AI extracts fields + suggests service type
- Generate quote and save lead/quote with channel=tiktok and attribution=manual_dm

BACKEND/API + WEB QUOTE FORM (MVP)
Monorepo:
- /apps/mobile (existing Expo app)
- /apps/api (Node.js + Express)
- /apps/web (Quick Quote page)
- /packages/shared (types)

DB: Prisma with Postgres preferred, fallback SQLite.

Prisma Models (minimum)
- User
- BusinessProfile
- PricingSettings
- ChannelConnection (channel enum: instagram, facebook, tiktok)
- Conversation
- Message
- OptOut
- Lead
- Quote
- Job (optional)
- AttributionEvent (eventType enum: click, form_submit, quote_created, job_won)

META INTEGRATION
Implement real OAuth endpoints:
- GET /auth/meta/start
- GET /auth/meta/callback
Store tokens encrypted at rest using ENCRYPTION_KEY env var.
Webhook:
- GET /webhooks/meta (verification handshake)
- POST /webhooks/meta (messages)
Processing:
1) upsert Conversation
2) insert inbound Message
3) if inbound contains opt-out keywords -> OptOut and stop
4) AI intent classify
5) if intent >= threshold AND automation enabled:
   - generate reply (<320 chars) + Quick Quote link
   - send reply via Meta messaging endpoint (implement real code path; allow stub in dev)

Quick Quote Link:
https://YOUR_WEB_APP_DOMAIN/q?u={userId}&ch=instagram&cid={conversationId}&mid={messageId}&utm_source=instagram&utm_medium=dm&utm_campaign=autoquote

QUICK QUOTE WEB PAGE (/q)
- Dark-friendly design, shows business logo/name
- On load: create AttributionEvent “click”
- Form captures: name, phone, email, zip, beds, baths, sqft, service type, frequency
- On submit:
  - POST /api/public/quotes/create including attribution params
  - compute quote using PricingSettings
  - create Lead + Quote
  - record AttributionEvent “quote_created”
- Show quote amount + breakdown + CTA “Call/Text to Book” (business phone)

AI FUNCTIONS (OpenAI)
- intentClassifier(text) -> { intent:boolean, confidence:0-1, category }
Categories: pricing_request, booking_request, service_area, general_question, spam
- replyGenerator(text, businessProfile, quoteLink) -> short, friendly reply
Rules:
- Under 320 chars
- 1 question max
- Include link when intent=true
- If spam, no reply

QUOTE ENGINE
computeQuote(input, pricingSettings):
- base = baseRate + bedrooms*bedroomsWeight + bathrooms*bathroomsWeight OR pricePerSqft*sqft (support either)
- multiply for deep/moveout
- enforce minPrice
Return amountCents + breakdown

SECURITY/COMPLIANCE
- Encrypt tokens
- Do not log tokens
- Opt-out compliance: honor STOP/UNSUBSCRIBE/QUIT
- Rate limit webhooks
- Validate Meta webhook signature if possible

DEV/STUB MODE (REQUIRED)
Provide a dev-only way to test onboarding without Meta approval:
- A “Simulate inbound DM” button in Social > Test screen (only when DEV=true)
- It triggers the same pipeline (intent -> auto-reply -> link -> attribution) using a mock conversation and logs.

DELIVERABLES
1) Social menu option integrated into existing app navigation
2) Full onboarding wizard “Turn on your AI Sales Assistant” with magic-moment Test DM
3) API + DB + web quote form working end-to-end
4) README with env vars + Meta setup + stub testing steps

DO NOT
- Do not rebuild the entire app
- Do not change existing UI styling drastically
- Do not fake TikTok DM APIs
- Do not add social scheduling or content calendars
