You are a senior React Native (Expo) engineer working inside my existing app “QuotePro” in Replit. I want an App Store-safe UI modernization (minimal risk). Do NOT create a new app. Do NOT change navigation routes. Do NOT refactor screen layouts. Focus ONLY on adding a Material 3 UI system and upgrading core UI primitives.

GOAL (APP STORE SAFE)
Implement these 3 changes only:
1) Add React Native Paper and wrap the app in PaperProvider with a custom Material 3 dark theme.
2) Standardize core UI primitives (Button + TextInput) using Paper components with minimal code changes.
3) Modernize the existing bottom tab bar styling (same tabs/routes) to look more premium.

CONSTRAINTS
- Keep the app compiling and running.
- Minimal diffs; avoid sweeping refactors.
- Keep all screen layouts as-is.
- Reuse existing navigation structure.
- Do not add backend/auth/payment changes.
- If the app already has a custom Button/Input component, update those components to use Paper internally so all screens benefit without editing every screen.
- If there is no shared Button/Input component, create lightweight wrappers and gradually replace only where easy (starting with Home + Quote screens).

STEP 1: REPO SCAN
Scan the repo and identify:
- Root entry file (App.tsx/App.js)
- Navigation entry (BottomTabs / TabNavigator file)
- Existing shared UI components (Button, Input, Card, Text, etc.)
- Existing theme/colors constants

Print a short plan listing the exact files you’ll modify/add before making changes.

STEP 2: INSTALL (Expo friendly)
Install:
- react-native-paper
- react-native-safe-area-context (if needed)
- react-native-vector-icons (if needed) OR use @expo/vector-icons
Use `npx expo install` where appropriate.

STEP 3: THEME
Create `src/theme/theme.ts` (or match existing structure) with a Material 3 dark theme:
- Define colors: background, surface, primary (accent), outline, text
- Set roundness (e.g. 14–18)
- Make it cohesive with current dark/navy vibe (do NOT switch to bright colors)
Wrap the app in:
<PaperProvider theme={theme}>...</PaperProvider>

STEP 4: CORE PRIMITIVES
Preferred approach:
- If there is an existing shared Button component, update it to render Paper <Button>.
- If there is an existing shared Input/TextField component, update it to render Paper <TextInput>.
- Ensure props are compatible (onPress, loading, disabled, mode, etc.)
- Map existing variants (primary/secondary/outline) to Paper modes (contained/outlined/text).

If there are no shared components:
- Create:
  - `src/components/ui/AppButton.tsx`
  - `src/components/ui/AppTextInput.tsx`
and refactor ONLY Home screen + Quote screens to use them for this release.

DO NOT rewrite every screen. Keep this surgical.

STEP 5: BOTTOM TAB BAR (STYLE ONLY)
In the existing bottom tab navigator:
- Keep the same tabs/routes in the same order.
- Update styling to feel premium:
  - height ~78–88
  - rounded top corners (20–28)
  - subtle elevation/shadow
  - background uses theme surface color
  - active icon uses theme primary
  - inactive icon uses muted gray
  - label slightly smaller with higher contrast on active
Optionally add a subtle active indicator/pill behind the focused icon using a wrapper View (no heavy animation required).

STEP 6: SMOKE CHECK
- Run the app and ensure navigation works.
- Ensure primary actions and input fields look consistent.
- Ensure no screens are broken.

DELIVERABLES
1) Working PaperProvider + theme file.
2) Updated shared Button/Input components or wrappers.
3) Updated bottom tab styling (routes unchanged).
4) List of files changed and where to tweak theme colors.

Start now: scan repo, show file plan, then implement.
