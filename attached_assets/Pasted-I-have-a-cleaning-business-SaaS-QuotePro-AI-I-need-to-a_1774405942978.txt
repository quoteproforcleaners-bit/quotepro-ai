I have a cleaning business SaaS (QuotePro AI). I need to add 
revenue-optimizing features to beat Jobber and ZenMaid.

1. CONVERT FREE TRIAL TO TIME-BASED
   Currently: free tier allows 3 total quotes ever.
   Change to: 14-day unlimited trial, then lock to free tier limits.
   
   - Add trialStartedAt: timestamp to users table. Set it on first login.
   - Add isInTrial(): boolean helper that returns true if 
     NOW() < trialStartedAt + 14 days
   - In the quota check middleware (POST /api/quotes), if isInTrial() 
     is true, skip the quota limit entirely
   - On the frontend, show a trial countdown banner:
     "Day X of 14 — upgrade before your trial ends to keep 
     unlimited quotes and AI features"
   - On day 11, 12, 13 show the banner in amber. Day 14 in red.

2. REFERRAL SYSTEM
   - Add to users table: referralCode (text, unique, auto-generated)
     and referredBy (text, FK to users.referralCode)
   - On registration, accept an optional ?ref= query param and 
     store the referredBy value
   - When a referred user upgrades to any paid plan, 
     give the referrer a 30-day credit:
     Add referralCreditsMonths: integer to users table
     Deduct one credit before charging their next Stripe invoice
     via Stripe's invoice.upcoming webhook + add invoice credit
   - Add GET /api/referrals — returns { referralCode, referralUrl, 
     referredCount, creditsEarned }
   - Add a Referral tab to SettingsPage showing their unique link, 
     share buttons (copy, SMS, email), and credits earned

3. AI FOLLOW-UP TASTE ON STARTER
   Currently: AI follow-ups are Growth-only.
   Change: Starter users get 3 AI follow-ups per month as a taste.
   
   - Add aiFollowUpsUsedThisMonth: integer to users table
   - Reset it in the monthly quota reset cron
   - In the follow-up route, if tier is 'starter' and 
     aiFollowUpsUsedThisMonth >= 3, return a 403 with:
     { error: 'limit_reached', message: 'Upgrade to Growth for 
     unlimited AI follow-ups', upgradeUrl: '/pricing' }
   - On the frontend, show remaining count: "2 AI follow-ups left 
     this month — upgrade for unlimited"

4. SUCCESS-MOMENT UPGRADE NUDGES
   Add these contextual upgrade triggers on the frontend:

   a) Quote accepted event:
      If user is on free/starter, show a modal:
      "Your quote was accepted! 🎉 Upgrade to Growth to 
      automatically follow up on the 3 quotes sitting in your 
      pipeline right now."
      CTA: "Upgrade to Growth — $49/mo"

   b) Revenue milestone:
      When dashboard shows lifetime revenue crosses $1,000, $5,000, 
      $10,000 — show a congratulations modal with an upgrade CTA 
      relevant to the next tier's features.

   c) Quote viewed but not accepted after 24h:
      Show an in-app notification: "A customer viewed your quote 
      but hasn't responded. Upgrade to Growth to send an AI 
      follow-up automatically."

5. MOBILE FEATURE PARITY — LEAD FINDER
   The Lead Finder feature currently only exists on web. 
   Add it to the React Native mobile app:
   
   - Create mobile/screens/LeadFinderScreen.tsx
   - Fetch from existing GET /api/lead-finder/leads endpoint
   - Show a list of leads with name, platform, status
   - Tap a lead → show the AI-generated reply options 
     (from POST /api/lead-finder/leads/:id/generate-replies)
   - Add "Copy reply" and "Mark contacted" actions
   - Add LeadFinder tab to the bottom tab navigator (Pro users only)
   - Show a ProGate paywall for non-Pro users

Show every new file, every schema change, every migration, 
and every frontend component created or modified.