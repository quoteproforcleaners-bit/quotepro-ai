You are Replit AI working inside my existing Expo/React Native iOS app “QuotePro” with an existing backend (Node/Express or similar). Implement a robust, cross-email-client quote acceptance system for 3 pricing options (Good/Better/Best) where the customer can accept in ONE TAP via a tokenized link, and immediately (same flow) choose a recurring frequency (one-time / weekly / biweekly / monthly). This must reliably update the quote status in my database and instantly notify the business owner inside the app.

CRITICAL CONSTRAINTS
- Email clients cannot reliably support true in-email interactive acceptance without links. Therefore implement a tokenized acceptance URL that opens a minimal web confirmation page (fast, mobile-first) and does NOT redirect into the app.
- Customer should not need to create an account or log in.
- After acceptance, business owner must see the quote marked as ACCEPTED in the app, including which option was chosen, chosen frequency, timestamp, and customer info.
- Keep UX extremely fast: 1 tap from email → confirm page → accepted + frequency.

FEATURES TO BUILD

1) Data Model Updates
Update Quote model:
- status: 'DRAFT' | 'SENT' | 'ACCEPTED' | 'DECLINED'
- acceptedAt (timestamp)
- acceptedOptionId ('good'|'better'|'best' or UUID)
- acceptedFrequency: 'one_time' | 'weekly' | 'biweekly' | 'monthly' | null
- acceptanceToken (secure random token; store hashed if feasible)
- acceptanceTokenExpiresAt (e.g., 14 days)
- acceptedSource: 'email_link' | 'web' | 'manual' (optional)
- acceptedNotes (optional text, for customer note)

Ensure quote stores 3 options:
- options: [{ id:'good', title, total, description, includedItems[] }, ...] with totals.

Add/confirm Customer model fields:
- name, email, phone (phone optional)
- address (optional)

Add SalesRecommendations model/table keyed by quoteId:
- recommendations: [{ type, title, rationale, suggestedDate, actionPayload, status:'open'|'done' }]

2) Email Template: 3 Options + Accept Buttons (Preselect Option)
When sending a quote email:
- Include a clean HTML email that shows:
  - Header: “Your Cleaning Quote”
  - Good/Better/Best cards with totals + bullet inclusions
  - Under each option: big CTA button “Accept This Option”
- Each button links to:
  https://<YOUR_DOMAIN>/accept/<token>?option=good
  https://<YOUR_DOMAIN>/accept/<token>?option=better
  https://<YOUR_DOMAIN>/accept/<token>?option=best
- Include fallback text:
  “If buttons don’t work, reply with 1 (Good), 2 (Better), or 3 (Best).”
(Only implement reply parsing if inbound email handling exists; otherwise keep as a human fallback.)

3) Public Acceptance Flow (Minimal Web Page, No App Redirect)
Backend routes:

A) GET /accept/:token
- Renders a minimal, mobile-first page (HTML/React/Next/templated—use whatever exists) that:
  - Validates token & expiry
  - Shows selected option card (preselected from query param option)
  - Shows an option switcher so customer can change option if they want
  - Shows “Choose your frequency” selector:
      ( ) One-time
      ( ) Weekly
      ( ) Biweekly
      ( ) Monthly
    Default:
      - If quote has a recommended frequency, preselect it; else default to “One-time”
  - Optional small inputs (keep minimal):
      - Phone number (optional; only if missing)
      - Note to the cleaner/business (optional)
  - Primary CTA: “Confirm Acceptance”
  - Secondary link: “Questions? Reply to this email.”

B) POST /api/quotes/accept
- Body: { token, optionId, frequency, phone?, note? }
- Validates:
  - token exists, not expired, maps to a quote
  - optionId is one of the quote’s options
  - frequency is valid enum
- Writes:
  - quote.status = 'ACCEPTED'
  - quote.acceptedAt = now
  - quote.acceptedOptionId = optionId
  - quote.acceptedFrequency = frequency
  - quote.acceptedSource = 'email_link'
  - quote.acceptedNotes = note (optional)
  - If phone provided and customer missing phone, store it.
- Idempotency:
  - If already ACCEPTED, return success and the stored acceptance values.
- Response:
  { success:true, quoteId, acceptedOptionId, acceptedFrequency }

C) GET /accept/success (or render inline success state)
- After POST success, show:
  “✅ Accepted — you’re all set!”
  Summary:
   - Option chosen + total
   - Frequency chosen
  Small note:
   “We’ll follow up shortly to confirm scheduling.”

4) Owner Notification + In-App Update
When quote becomes ACCEPTED:
- Send push notification to owner device(s) if push exists:
  “✅ Quote accepted: <Customer Name> chose <Better> ($___) — <Biweekly>”
- Update app UI:
  - Quote list shows ACCEPTED badge + selected option + frequency
  - Quote detail shows acceptance timeline event
- If no push exists:
  - Implement in-app Activity Feed entry + unread badge.

5) Revenue-Focused Underpricing Detector (Quote-Time)
Add/confirm a pricing sanity check:
- Settings (per business owner):
  - laborCostPerHour (default if missing)
  - targetMarginPercent (default 35%)
  - productivityRate (optional) used to estimate labor hours (e.g., sqft per hour)
- Compute:
  - estimatedLaborHours from sqft/beds/baths/add-ons via configurable formula
  - estimatedLaborCost = hours * laborCostPerHour
  - suggestedMinimumPrice = estimatedLaborCost / (1 - targetMarginPercent)
- UI:
  - If any tier is below suggestedMinimumPrice threshold, show warning:
    “⚠️ This quote may be underpriced”
    “Suggested minimum: $X to hit 35% margin”
    Button: “Adjust tiers to profitable range”
    - Implement adjustment: apply a multiplier or add delta evenly across tiers while preserving Good < Better < Best.

6) Post-Accept “Sales Recommendations” (LTV Engine) + Auto-Recurring Nudge
On acceptance event, automatically generate recommendations and show them to the owner in the Quote Detail screen under a section called “Revenue Playbook”.

Generate 3–6 recommendations (cleaning-specific, revenue-only), for example:
- TYPE: schedule_followup_deep_clean
  Title: “Schedule a Deep Clean in 6 months”
  Rationale: “Deep cleans drive higher ticket sizes and reduce churn.”
  suggestedDate: acceptedAt + 6 months
  Action: create internal reminder + optional SMS/email template

- TYPE: convert_to_recurring
  Title: “Confirm recurring schedule”
  Rationale: “Recurring customers increase LTV and stabilize revenue.”
  Action: one-tap “Send scheduling text” with chosen frequency included

- TYPE: quarterly_addons
  Title: “Offer quarterly add-ons”
  Rationale: “Quarterly upsells raise average revenue per customer.”
  suggestedDate: acceptedAt + 90 days
  Action: one-tap upsell message

- TYPE: referral_ask
  Title: “Ask for a referral after first clean”
  suggestedDate: firstServiceDate + 1 day (if you track service date; otherwise acceptedAt + 7 days)
  Action: one-tap referral SMS/email

IMPORTANT: Use acceptedFrequency to tailor recommendations.
- If frequency is one_time, recommend “Upgrade to Biweekly” with incentives.
- If weekly/biweekly/monthly, recommend “Lock in recurring” and “Deep clean at 6 months”.

7) Optional (If Easy): “Preferred Days/Times” Micro-Field
On acceptance page include optional quick select:
- Preferred days: Mon–Sun chips
- Preferred time window: Morning / Afternoon / Evening
Store on quote as acceptedPreferences JSON.
Show it in the app for scheduling follow-up.

IMPLEMENTATION STEPS (DO IN ORDER)
1) Scan repo for:
  - Navigation entry point
  - Quote model + storage
  - Email sending service and current quote email template
  - Quote builder/preview screens and actions (save/send/export)
  - Push notifications implementation (Expo Push / FCM / APNS)
2) Implement data model migrations/updates.
3) Update quote email template to include 3 options + accept links.
4) Implement /accept/:token page + /api/quotes/accept endpoint.
5) Implement acceptance success page/state.
6) Wire acceptance event to notifications + app UI refresh.
7) Implement Underpricing Detector in quote builder.
8) Implement Revenue Playbook recommendations generation on acceptance and show in app.
9) Test end-to-end flows thoroughly.

ACCEPTANCE TESTS
- Fresh quote email contains 3 pricing options + “Accept” buttons.
- Customer taps accept → loads fast page with option preselected.
- Customer selects frequency (or keeps default) → confirms acceptance.
- Quote updates to ACCEPTED with option + frequency stored.
- Owner sees ACCEPTED in app immediately (push if available).
- Underpricing detector flags low tiers and can auto-adjust.
- Revenue Playbook appears after acceptance and suggests next revenue actions.

NON-NEGOTIABLES
- No app redirect/deep link required for acceptance.
- Customer does not need to log in.
- Must work in Gmail, Apple Mail, Outlook.
- Minimal, polished UX.
- Idempotent acceptance (multiple taps won’t break anything).

Now start by scanning the codebase and telling me:
- What backend framework and routing you use
- How emails are sent (provider + template location)
- Where quotes/options are stored
Then implement the feature set with minimal disruption and list all files changed/added.