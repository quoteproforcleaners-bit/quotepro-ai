You are an elite iOS product designer + React Native (Expo) engineer. My app (QuotePro) has a dark navy theme that looks clean but feels a bit flat/dull. I want you to add depth, hierarchy, and “premium SaaS” polish WITHOUT redesigning the layout or changing any logic. This is purely a visual refinement pass focused on: layered surfaces, subtle gradients, better shadows, and tasteful glows for key CTAs.

PRIMARY GOALS
1) Make the UI feel more dimensional (clear foreground/background layers).
2) Increase visual hierarchy (important cards/CTAs pop more).
3) Add subtle lighting (gradients + glows) while keeping a modern, restrained look (no neon).
4) Keep performance good and changes minimal.

SCOPE (must update these areas)
- Home screen cards: “$ at risk” card, Follow-Up Streak card, Ask QuotePro input card, Unlock AI features card, bottom quick actions.
- Shared card component styles (if you have reusable Card / Surface components, refactor there).
- Primary CTA styles (buttons).
- Background for main screens.

IMPLEMENTATION REQUIREMENTS
- Do NOT change navigation, state, data, or business logic.
- Do NOT change copy text, only styling.
- Use a centralized theme token file for colors and elevation so it’s consistent.
- Use expo-linear-gradient for gradients (install if needed).
- Add subtle shadow/elevation in a platform-correct way (iOS shadow props + Android elevation).

DESIGN SYSTEM UPDATES (use EXACT tokens below)
Create/Update: /src/theme/colors.ts and /src/theme/elevation.ts (and export them from /src/theme/index.ts)

COLORS
Background:
- bgTop: #0A101C
- bgBottom: #121E31

Surfaces:
- surface0: #0F1A2A   (base card)
- surface1: #111C2E   (raised card)
- surface2: #162338   (selected / emphasized card)
- border: #22314A
- divider: #1B2940

Text:
- textPrimary: #F3F6FF
- textSecondary: #B8C2D6
- textMuted: #8A96B0

Brand Blue:
- brand: #2F7BFF
- brandSoft: rgba(47,123,255,0.16)
- brandGlow: rgba(47,123,255,0.18)

Revenue/Warning (for “at risk” ONLY):
- warning: #F59E0B
- warningSoft: rgba(245,158,11,0.16)
- warningBorder: rgba(245,158,11,0.35)

ELEVATION TOKENS (create elevation.ts)
- e0: { shadowColor: #000, shadowOpacity: 0.0, shadowRadius: 0, shadowOffset: {width:0,height:0}, elevation: 0 }
- e1: { shadowColor: #000, shadowOpacity: 0.18, shadowRadius: 10, shadowOffset: {width:0,height:6}, elevation: 2 }
- e2: { shadowColor: #000, shadowOpacity: 0.22, shadowRadius: 18, shadowOffset: {width:0,height:10}, elevation: 4 }
- e3: { shadowColor: #000, shadowOpacity: 0.28, shadowRadius: 26, shadowOffset: {width:0,height:14}, elevation: 6 }

BACKGROUND
Implement a reusable ScreenBackground wrapper using expo-linear-gradient:
<LinearGradient colors={[bgTop, bgBottom]} start={{x:0,y:0}} end={{x:0,y:1}} style={{flex:1}}>
  {children}
</LinearGradient>
Use it on the Home screen and any other main screens using this theme.

CARD DEPTH RULES
- Default cards use surface0 + border + e1 elevation.
- Key cards (like “Follow-Up Streak” and “Unlock AI”) use surface1 + e2 elevation.
- Selected or emphasized areas can use surface2 with brandSoft overlay.

“$ AT RISK” CARD (make it feel urgent and premium)
- Use a subtle tinted gradient background:
  gradient from #1B2436 (top) to #121B2B (bottom)
- Border uses warningBorder
- Icon/accent uses warning
- “Follow Up Now” CTA inside the card should be a high-contrast pill:
  background: rgba(245,158,11,0.18) with a thin warningBorder and text warning
  AND add a subtle warning glow (shadow) behind it if possible (very subtle).

GLOW (tasteful)
Add a subtle outer glow ONLY for primary CTAs and selected states:
- Use brandGlow for blue CTAs (blur via shadowRadius).
- Do NOT add glow everywhere.

BUTTONS
- Primary button: brand background, textPrimary, 14-16px radius, e2 elevation.
- Secondary/outline: transparent background, 1px border brand, text brand, brandSoft background on press.

MICRO-INTERACTIONS (simple, optional but recommended)
- Add Pressable style that slightly scales down on press (e.g., transform scale 0.98) for primary CTAs.
- Add subtle fade-in on Home screen mount for top cards (opacity from 0 to 1 over 200ms). Keep minimal.

WHAT TO CHANGE IN CODE
1) Search for all card styles on Home screen and refactor them to use the new tokens + elevation.
2) If you have a reusable Card component, update it to accept variants: "base" | "raised" | "emphasis" | "warning".
3) Update the “$ at risk” component to use the warning variant and gradient.
4) Update the main screen container to use ScreenBackground.
5) Ensure text colors use textPrimary/textSecondary/textMuted tokens (no random opacity whites).

OUTPUT
- Provide code for all files created/modified in full.
- List the files changed.
- Confirm the Home screen now has clear depth: background gradient, layered surfaces, and emphasized “at risk” card + CTA.

Start by inspecting the current styling approach (StyleSheet vs styled-components vs Tailwind) and implement accordingly.