I have a Node.js + Express + TypeScript backend for 
QuotePro AI. I need to fix specific security gaps 
identified in a professional audit.

1. PROMPT INJECTION SANITIZATION
   In server/aiClient.ts, before any user-supplied 
   content is inserted into an AI prompt:
   
   Create: server/promptSanitizer.ts
   
   export function sanitizeForPrompt(input: string): string {
     // Remove common injection patterns
     const injectionPatterns = [
       /ignore\s+(all\s+)?previous\s+instructions?/gi,
       /you\s+are\s+now\s+/gi,
       /forget\s+(all\s+)?previous/gi,
       /system\s*:/gi,
       /\[system\]/gi,
       /\[user\]/gi,
       /\[assistant\]/gi,
       /<\|im_start\|>/gi,
       /<\|im_end\|>/gi,
       /###\s*instruction/gi,
       /act\s+as\s+(if\s+you\s+are|a)\s+/gi,
       /pretend\s+(you\s+are|to\s+be)\s+/gi,
       /reveal\s+(your\s+)?(system\s+)?prompt/gi,
       /show\s+me\s+your\s+(system\s+)?instructions/gi,
     ]
     
     let sanitized = input
     injectionPatterns.forEach(pattern => {
       sanitized = sanitized.replace(pattern, '[removed]')
     })
     
     // Truncate at 2000 chars max for any user input
     return sanitized.slice(0, 2000)
   }
   
   Apply sanitizeForPrompt() to ALL user-supplied fields 
   before they enter any prompt in:
   - agent-chat: the user message content
   - walkthrough-extract: the raw notes text
   - communication-draft: customer notes/context
   - generate-message: any customer-supplied content
   
   Log any sanitization that removed content:
   trackEvent with 'PROMPT_INJECTION_DETECTED' + 
   the original content (truncated) for review.

2. STRIPE WEBHOOK VERIFICATION AUDIT
   In the Stripe webhook handler 
   (POST /api/subscription/webhook):
   
   Confirm this EXACT pattern is implemented:
   
   // Route must use express.raw() NOT express.json()
   router.post('/webhook', 
     express.raw({ type: 'application/json' }),
     async (req, res) => {
       const sig = req.headers['stripe-signature']
       let event
       
       try {
         event = stripe.webhooks.constructEvent(
           req.body,  // must be raw Buffer, not parsed JSON
           sig,
           process.env.STRIPE_WEBHOOK_SECRET
         )
       } catch (err) {
         console.error('Webhook signature failed:', err)
         return res.status(400).json({ 
           error: 'Invalid signature' 
         })
       }
       
       // ONLY process after verification
       switch (event.type) { ... }
     }
   )
   
   If this pattern is not exactly implemented, fix it.
   Add an integration test that POSTs with a bad 
   signature and confirms 400 is returned.

3. REFERRAL SYSTEM FRAUD PROTECTION
   Find the referral credit logic (Sprint 6 from 
   previous audit added this — update it):
   
   Add these fraud checks before crediting a referral:
   
   a) Require 30 days of active paid subscription 
      before crediting the referrer:
      Only credit when referred_user.subscription_start 
      + 30 days has passed AND subscription is still active
   
   b) Same-IP fraud detection:
      Store ip_address on the users table (captured at signup)
      If referrer and referred share the same IP address:
      → do NOT auto-credit
      → flag for manual review in admin dashboard
      → log to analyticsEvents: 'REFERRAL_FRAUD_SUSPECTED'
   
   c) Cap referral credits at 6 months maximum:
      COUNT credits earned by referrer
      If count >= 6: skip credit, log 'REFERRAL_CAP_REACHED'
   
   d) Email pattern detection:
      If referred user email domain matches referrer email 
      domain (e.g., both @gmail.com is fine, but both 
      @samecompany.com is suspicious):
      → flag for review but don't auto-block

4. STARTER TIER AI CAP HARDENING
   Find the AI follow-up send route. Confirm and fix 
   this exact server-side enforcement:
   
   // In the follow-up send route, BEFORE sending:
   const user = await getUserById(req.user.id)
   
   if (user.subscriptionTier === 'starter') {
     if (user.aiFollowUpsUsedThisMonth >= 3) {
       return res.status(403).json({
         error: 'limit_reached',
         message: 'Upgrade to Growth for unlimited AI follow-ups',
         upgradeUrl: '/app/pricing'
       })
     }
   }
   
   // AFTER successful send:
   await db.update(users)
     .set({ 
       aiFollowUpsUsedThisMonth: 
         sql`ai_follow_ups_used_this_month + 1` 
     })
     .where(eq(users.id, req.user.id))
   
   // In the monthly reset cron:
   await db.update(users)
     .set({ aiFollowUpsUsedThisMonth: 0 })
   // This must be idempotent — running twice 
   // should not double-reset or cause issues.

5. API KEY HASHING AUDIT
   Find where API keys are created and verified 
   (api_keys table, apiRouter.ts).
   
   Confirm and fix:
   - API key CREATION: generate a random 32-byte key, 
     store SHA-256 hash in DB, return the PLAIN key 
     to the user ONCE (never store the plain key)
   - API key VERIFICATION: on incoming request, 
     SHA-256 hash the provided key, compare to DB
   - Passwords use bcrypt (slow) — correct
   - API keys use SHA-256 (fast) — correct
   
   If bcrypt is being used for API keys, migrate:
   - On each API key use, if it verifies via bcrypt, 
     re-hash with SHA-256 and update the DB record
   - After 30 days, all keys will have migrated 
     without requiring users to rotate keys

6. ADMIN SECURITY DASHBOARD
   Add to GET /api/admin/security:
   {
     promptInjectionAttempts: number,  // last 30 days
     failedWebhookSignatures: number,
     suspectedReferralFraud: number,
     failedLoginAttempts: number,  // from rate limiter logs
     apiKeysIssued: number,
     apiKeysRevoked: number
   }
   Requires X-Admin-Key header.

Show every changed file, every security fix location,
and a SECURITY.md file documenting all implemented 
security measures for future engineers.