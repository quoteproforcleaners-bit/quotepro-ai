You are Replit AI. Update the existing Expo/React Native iOS app “QuotePro” onboarding / first screen to maximize “I need this” + “this will make me more money” conversion, while keeping the current design language (dark premium UI, blue accents, modern rounded components) and preserving the current navigation + auth logic.

GOAL
Make the first screen feel like a revenue weapon, not a generic utility.
Increase taps on the primary CTA (“Create a Free Quote”) and reduce drop-off.

NON-NEGOTIABLES
- Keep “No account needed to get started.”
- Keep the Language toggle (English / Español / Português / Русский) at the top.
- Primary CTA remains above the fold.
- No new dependencies unless absolutely necessary.
- Must look great on iPhone AND iPad (reviewers use iPad Air 11").
- Add copy variants as constants so I can A/B test later.

WHAT TO CHANGE (HIGH IMPACT)

1) Replace the Subheadline With Revenue + Urgency
Current: “Professional Quoting For Cleaning Businesses”
Replace with one of these (make it easy to switch via a constant):

Variant A (default):
“Close More Cleaning Jobs in 30 Seconds”

Variant B:
“Stop Losing Jobs to Better Quotes”

Variant C:
“Turn Quotes Into Closed Jobs With AI”

Variant D (only if we can support the claim):
“Add $5,000/Month With Better Quotes”

Implementation:
- Create a constant `ONBOARDING_HEADLINE_VARIANT` with values 'A'|'B'|'C'|'D'
- Create a map/object holding the strings and render the selected one.

2) Add a “Tension → Relief” Micro-copy Line (1 sentence)
Under the subheadline add a small line (subtle but readable):
“Most cleaning businesses lose jobs because their quotes look unprofessional — fix it in minutes.”

Make this also a constant so it can be edited later.

3) Improve CTA Copy for Ownership + Action
Change primary button label from:
“Create a Free Quote”
to:
“Create My First Quote”
(keep icon)

Add a smaller line directly under the button (already present but improve copy):
Current: “No account needed to get started”
Keep but slightly stronger:
“No account needed — build your first quote now”

4) Add Micro Social Proof (No fake numbers)
Add a small line beneath the “No account needed” line, with neutral wording (no fabricated claims):
“Built by a real cleaning business owner.”
Optional second line:
“Designed to help you win more jobs and charge what you’re worth.”

Do NOT claim “trusted by X” unless we pull real numbers from a live metric source.
If we can safely read the current install count from a config value you already maintain, then we can display “Used by X+ cleaners” but ONLY if X is real and passed from backend/config.

5) Replace Feature Labels With Outcomes (keep icons)
Currently you show:
Instant Quotes / Customer Management / Job Scheduling / Growth Tools
Change to revenue/outcome phrasing:

- Win More Jobs
- Never Miss Follow-Ups
- Look More Professional
- Grow Revenue Faster

Keep the same icon layout and spacing; only change text.

6) Add a Lightweight “Proof Preview” Card (Optional but recommended)
Add a small preview card between the Sign In button area and the feature icons OR just above the feature icons (whichever fits best without pushing CTA below fold). This card should look like a quote preview snippet (NOT functional, just UI):
- Title: “Preview”
- A tiny “Good / Better / Best” row with example prices (generic, e.g., $180 / $240 / $320) with no promises.
- A small “Looks professional. Sends fast.” line.
This should be a simple View with text—no PDF generation, no complex components.

7) Keep Sign In Secondary
Keep “Sign In to Your Account” but ensure it’s visually secondary:
- Lower contrast outline
- Slightly smaller height than primary button
- Add helper link “Why create an account?” but update copy to be benefit-based:
Change link text to:
“Save quotes + send to customers →”
(keep the existing handler / modal)

DESIGN REQUIREMENTS
- Maintain current dark gradient background style.
- Improve visual hierarchy:
  - App name and new subheadline prominent
  - CTA is the brightest element
  - Social proof subtle
- Reduce “empty dead space” by tightening vertical spacing and adding the Preview card.
- Ensure dynamic type / accessibility:
  - Use scalable fonts but prevent layout breaking.
  - Buttons remain tappable.

TECHNICAL REQUIREMENTS
- Identify the current component file for the first screen (e.g., WelcomeScreen, OnboardingScreen, LandingScreen).
- Implement copy changes via constants at top of file (or a small config file).
- Ensure iPad layout looks centered and not stretched:
  - Use maxWidth container (e.g., 520) and center align on large screens.
- Test: app runs without warnings or layout overflow.

DELIVERABLE
- Update the first screen with the above changes.
- Provide the exact files changed and a brief summary of what changed.
- Do not modify unrelated screens.

Start by locating the first screen component and implementing Variant A by default.