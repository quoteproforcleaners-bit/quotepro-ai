Replace all generic empty states across QuotePro with 
contextual, action-oriented prompts that guide new users 
toward their first meaningful action.

SCREENS TO UPDATE:

1. GrowthDashboardScreen (when all stats are zero):
- Detect "new user" state: totalQuotes === 0
- Replace the metrics grid with a single welcome card:
  Title: "Welcome to your Growth Dashboard"
  Body: "Your stats will appear here as you send quotes and 
  win jobs. Start by creating your first quote."
  CTA Button: "Create First Quote" → navigate to QuoteCalculator
  Style: centered card with theme.primary border

2. FollowUpQueue (when empty):
- Current: generic "No follow-ups" message
- New: 
  If 0 quotes sent: "Send your first quote to start 
  building your follow-up pipeline."
  CTA: "Create a Quote"
  If quotes sent but none pending: "You're all caught up! 
  Every quote has been followed up on."
  Icon: green check, no CTA needed

3. OpportunitiesScreen — Dormant tab (when empty):
- "No dormant customers yet. As you complete jobs, past 
  clients who haven't rebooked will appear here."
- CTA: "Schedule a Job" → navigate to JobScheduler

4. OpportunitiesScreen — Lost Quotes tab (when empty):
- "No lost quotes. When sent quotes expire without a 
  response, they'll appear here for recovery."
- CTA: "Send a Quote" → navigate to QuoteCalculator

5. ReviewsReferralsScreen (when reviewRequests is empty):
- "Ask your first customer for a review after completing 
  a job. Reviews help you win more business."
- CTA: "View Completed Jobs" → navigate to Jobs tab

6. ReactivationScreen (when no campaigns):
- "Create your first campaign to win back past customers 
  or recover lost quotes automatically."
- CTA already exists (New Campaign FAB) — just update the 
  empty state copy and add an illustration or icon

7. TasksQueue / GrowthTasks (when empty):
- "Your AI growth tasks will appear here once you start 
  sending quotes and completing jobs. Check back soon."
- No CTA needed

IMPLEMENTATION NOTES:
- Create a reusable <ActionEmptyState> component:
  Props: icon, title, description, ctaLabel, onCta
  Replaces all existing EmptyState usages in these screens
- Keep the existing EmptyState component for non-growth 
  screens (checklist empty, photos empty, etc.)

Test: New account with zero data sees helpful prompts 
on every screen instead of blank states.