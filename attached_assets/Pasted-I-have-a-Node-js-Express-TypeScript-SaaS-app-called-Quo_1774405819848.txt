I have a Node.js + Express + TypeScript SaaS app called QuotePro AI.
The backend is a single file: server/routes.ts (~14,800 lines).
Auth uses express-session + connect-pg-simple. Database is PostgreSQL 
via Drizzle ORM (schema in shared/schema.ts).

Make ALL of the following security fixes:

1. RATE LIMITING ON AUTH ENDPOINTS
   - Install express-rate-limit
   - Apply a limiter of 10 requests/minute to ALL of these routes:
     POST /api/auth/login
     POST /api/auth/register
     POST /api/auth/apple
     POST /api/auth/google
   - After 5 failed login attempts from the same IP, add a 
     15-minute block for that IP
   - Return HTTP 429 with message "Too many attempts. Try again later."

2. STRIPE WEBHOOK HMAC VERIFICATION
   - Find every route that handles incoming Stripe webhooks
   - Add stripe.webhooks.constructEvent(req.body, sig, secret) 
     verification BEFORE any business logic runs
   - If verification fails, return HTTP 400 and log the attempt
   - Use raw body parser (not JSON) on the webhook route — 
     Stripe requires the raw body for signature verification
   - Read the secret from process.env.STRIPE_WEBHOOK_SECRET

3. SESSION FIXATION FIX
   - On every successful login (all auth methods), call 
     req.session.regenerate() BEFORE writing user data to the session
   - This must happen in: /api/auth/login, /api/auth/apple, 
     /api/auth/google, /api/auth/exchange-token

4. REVENUECAT ENTITLEMENT RE-VERIFICATION
   - Find the route that processes RevenueCat subscription updates
   - After any subscription event, re-verify the entitlement 
     server-side by calling the RevenueCat REST API:
     GET https://api.revenuecat.com/v1/subscribers/{appUserId}
     with Authorization: Bearer {REVENUECAT_SECRET_KEY}
   - Only update users.subscriptionTier in the DB after server-side 
     confirmation — never trust the client payload alone

5. API KEY SCOPING
   - Find the api_keys table in shared/schema.ts
   - Add a "scopes" column (text array) if it doesn't exist:
     scopes: text('scopes').array().default(['read:quotes'])
   - Define these valid scopes:
     read:quotes, write:quotes, read:customers, write:customers,
     read:jobs, write:jobs, read:analytics
   - On every API route that accepts key-based auth, check that 
     the key's scopes array includes the required scope for that route
   - Return HTTP 403 "Insufficient scope" if the key lacks permission

Do not change any UI, business logic, or pricing calculations.
Show me a summary of every file changed and every route modified.