You are an expert senior mobile engineer + product engineer. We are building NEW FEATURES inside my existing iPhone app “QuotePro” (Expo/React Native project in Replit). Do NOT create a new app. Do NOT change existing navigation structure unless required. Add 3 features:

(1) MISSED REVENUE DETECTOR (Quotes that should be followed up)
(2) AI AUTO FOLLOW-UP ENGINE (scheduled follow-up messages for quotes)
(4) AI SALES ASSISTANT (chat-style assistant for replies/objections + message rewriting)

IMPORTANT CONSTRAINTS
- Keep the current app working. Minimal refactors.
- Use TypeScript if project already uses TS; otherwise JS is fine.
- Store data locally using AsyncStorage (or the storage system already used in the app). If the project uses SQLite already, use that instead. Prefer existing patterns.
- If any AI features are not live yet, build the UI + flows now and wire to a stub/mock that I can later connect to a real AI endpoint. The stub should generate realistic outputs deterministically.
- Do NOT require user accounts or backend.
- Add feature flags: `aiEnabled` (default false) and `aiLaunchEtaDays` (default 14). If aiEnabled is false, show “AI launches in ~1–2 weeks” and an Upgrade CTA.
- Add in-app upgrade paywall CTA placeholder (no real payments) that routes to an existing Upgrade screen if there is one, otherwise create a simple Upgrade screen.

UX / NAVIGATION
- Add a new tab OR menu item called “Revenue” for the Missed Revenue Detector (whichever matches existing design).
- Add a new tab OR menu item called “AI Assistant” for AI Sales Assistant.
- Inside each Quote detail screen, add a section called “Follow-ups” with timeline of scheduled follow-ups + a “Schedule Follow-ups” button.
- Keep UI consistent with existing components/styles. Reuse existing Button, Card, ListRow components if present.

DATA MODEL (use existing models and extend them)
We currently have Quotes, Customers, Jobs.
Extend Quote with:
- `status`: "draft" | "sent" | "won" | "lost"
- `sentAt?: string` ISO date
- `lastContactAt?: string` ISO date
- `followUps?: Array<{
    id: string
    scheduledFor: string
    channel: "sms" | "email"
    message: string
    status: "scheduled" | "sent" | "skipped"
    createdAt: string
    sentAt?: string
  }>`
- `closeProbability?: number` (0-100)
- `expectedValue?: number` (calculated from chosen tier or avg ticket)
- `aiNotes?: string`

(1) MISSED REVENUE DETECTOR — REQUIREMENTS
Create Revenue screen with:
- Summary cards:
  - “Unfollowed Quotes” (# of quotes sent > 48h ago with no lastContactAt update)
  - “Stale Quotes” (sent > 7 days ago not won/lost)
  - “High Value At Risk” (sum expectedValue of unfollowed + stale)
- A prioritized list called “Do These Next”:
  - Sort by expectedValue DESC, then by days since sent DESC
  - Each row: Customer name, expected value, days since sent, quick actions (Call / Text / Email / Open Quote)
- Filters:
  - last 7 / 30 / 90 days
  - min expected value slider (or simple min value input)
- Tapping an item opens the Quote detail

Detector logic:
- “Unfollowed”: quote.status = "sent" AND sentAt <= now-48h AND (lastContactAt missing OR lastContactAt <= sentAt)
- “Stale”: quote.status = "sent" AND sentAt <= now-7d
- “High Value”: expectedValue sum for all unfollowed + stale
Expected value:
- If quote has Good/Better/Best prices, use Better as default; otherwise use quote.total
- If missing, set expectedValue to 0.

(2) AI AUTO FOLLOW-UP ENGINE — REQUIREMENTS
In Quote detail:
- Show Follow-ups timeline list (scheduled + sent)
- Button: “Schedule Follow-ups”
When scheduling:
- Offer recommended cadence (editable):
  - 24h after sent: friendly check-in (sms)
  - 72h after sent: objection-handling (sms or email)
  - 7d after sent: last call (email)
- User can toggle each on/off and edit messages.
- When saved, create followUps entries with scheduledFor dates based on sentAt (if sentAt missing, use now and set sentAt).
Sending:
- Because there is no backend, implement “send” as:
  - For SMS: open native Messages app with prefilled text (use Linking URL scheme `sms:` or existing method)
  - For Email: open Mail composer via `mailto:`
- When user taps “Send now” on a scheduled item, mark it as sent and set `lastContactAt=now`.
- Also provide “Skip” action.

Message generation:
- If aiEnabled true: call a local stub `generateAiFollowUp(quote, customer, tone, step)` that returns a personalized message.
- If aiEnabled false: use high-quality templates with placeholders.

(4) AI SALES ASSISTANT — REQUIREMENTS
Create “AI Assistant” screen with:
- A dropdown to choose context:
  - Quote (select from recent quotes)
  - Customer (select from customers)
  - General
- A chat UI:
  - user types a message like: “Customer says this is too expensive” or paste their text
  - assistant returns:
    1) Suggested reply (SMS-length)
    2) Suggested reply (Email-length)
    3) 3 bullet “why this works”
    4) 3 alternative tones (Friendly, Direct, Luxury)
- Buttons under each suggestion: Copy, Open SMS, Open Email
- Quick action chips:
  - “Handle price objection”
  - “Follow-up after no response”
  - “Ask for photos/details”
  - “Upsell add-ons”
  - “Close the deal”
- If aiEnabled false, show the same UI but with a banner “AI launches in ~1–2 weeks” and generate using deterministic stub + templates.

IMPLEMENTATION DETAILS
- Add utility functions for date math (48h, 7d) without new heavy deps; use `date-fns` only if already installed.
- Add simple IDs with `Date.now().toString()` or existing uuid helper.
- Add tests only if the project already has a test setup; otherwise skip tests.
- Update types/interfaces and storage migration safely: if followUps missing, treat as empty array.
- Ensure all new screens are wired into existing navigation and accessible.

DELIVERABLES
1) Code changes implementing the three features.
2) A short “How to use” section in README.md for:
   - Revenue screen
   - Scheduling follow-ups
   - AI Assistant screen
3) A list of files changed/added.

Before coding:
- Inspect the existing project structure and identify:
  - where Quotes/Customers/Jobs are stored
  - where navigation is defined
  - existing components/styles
Then implement with the smallest changes possible, and keep the app compiling/running.

Now start by scanning the repo and showing me your plan + the exact files you will touch, then implement.
