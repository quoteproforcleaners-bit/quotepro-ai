You are Replit AI working inside my existing React Native / Expo iOS app “QuotePro for Cleaners”. Implement a NEW “Commercial Quoting” add-on that lives inside the existing Quote Builder. This must be cleanly separated from Residential quoting and must not break any existing flows.

HIGH-LEVEL GOAL
Add a Quote Type selector (Residential vs Commercial). When Commercial is selected, show a new Commercial Walkthrough mode (structured onsite walkthrough), generate labor-hour estimate, margin-protected pricing, and a professional commercial proposal (PDF) with Good/Better/Best tiers. This is an add-on feature (gated by subscription toggle/flag), but even without paywall logic, build it fully and wire it behind a feature flag.

NON-NEGOTIABLE APPLE COMPLIANCE
Before ANY request is sent to OpenAI, enforce an “AI Consent” gate:
- Show a modal explaining that user-entered data (customer names, addresses, job details, pricing inputs, notes) will be sent to OpenAI for AI features.
- Buttons: Decline / I Agree
- If Decline: AI features are disabled (still allow manual quoting).
- If Agree: persist consent in AsyncStorage and never show again.
- Wrap every OpenAI call with a check: if consent !== true, block and show modal.
(If consent modal already exists, reuse it and ensure ALL new commercial AI calls use it.)

TECH STACK ASSUMPTIONS
- React Native + Expo
- Local persistence using AsyncStorage (or existing storage layer)
- Existing quote screens and PDF generation exist for residential quotes
- Existing OpenAI client exists (gpt-5-nano) OR create a thin service wrapper using existing env vars

FEATURE FLAG / GATING
- Add a feature flag `commercialQuotingEnabled` (default true for now) located in a centralized config.
- Add a simple UI toggle under Settings: “Enable Commercial Quoting (Beta)” for internal testing.
- When disabled: hide Commercial option entirely or show it locked with “Upgrade” label (no purchase flow needed unless it already exists).

USER FLOW
1) From Quote Builder -> user taps “Create Quote”
2) First screen includes:
   - Quote Name (optional default: Customer + Date)
   - Customer selector (existing)
   - Quote Type selector: Residential | Commercial
   - Next

Residential flow continues unchanged.

Commercial flow:
3) “Commercial Walkthrough” wizard with steps and progress indicator:
   Step A: Site Basics
     - Business/Facility Name (defaults from customer/company)
     - Site Address (editable)
     - Facility Type (Office, Retail, Medical, Gym, School, Warehouse, Restaurant, Other)
     - Total Sq Ft (number)
     - # Floors (number)
     - After-hours required? (yes/no)
     - Access constraints (text)
   Step B: Areas & Counts
     - Bathrooms count
     - Breakrooms/kitchenettes count
     - Conference rooms count
     - Private offices count
     - Open areas count
     - Entry/lobby count
     - Trash points count
   Step C: Floors & Surfaces
     - Carpet % (0-100)
     - Hard floor % (0-100) auto-validates sum=100
     - Glass/Interior windows (None/Some/Lots)
     - High touch focus? (yes/no)
   Step D: Service Frequency & Timing
     - Frequency: 1x/week, 2x/week, 3x/week, 5x/week, daily, custom
     - Preferred days/time window
     - Duration per visit constraint (optional)
   Step E: Supplies & Equipment
     - Supplies provided by client? (yes/no)
     - Restroom consumables included? (yes/no)
     - Special chemicals required? (text)
   Step F: Notes & Photos (optional)
     - Notes (text)
     - Photo attachments placeholder (if app already supports uploads; otherwise stub UI)

4) Labor Estimate screen (core innovation)
Compute an estimated labor-hours-per-visit using a transparent model:
- Base minutes per 1,000 sq ft by facility type (use a small table with sensible defaults)
- Add minutes per bathroom, breakroom, trash point, glass level, high-touch
- Adjust by carpet/hard floor mix
- Show:
  - Estimated minutes/visit
  - Recommended # cleaners (ceil(estimated_minutes / target_minutes_per_cleaner))
  - Estimated labor hours/visit
Allow user to override:
- Override labor hours/visit (editable)
- Target minutes per cleaner (default 120)
- Store both raw estimate + override

5) Pricing / Margin Engine screen
Inputs:
- Hourly labor rate (default pulled from user settings if exists)
- Overhead % (default 0.15)
- Target profit margin % (default 0.20)
- Supplies surcharge (optional fixed or %)
- Frequency discount rules (optional)
Outputs:
- Recommended price per visit
- Recommended monthly price (based on frequency)
- Show a “Margin Health” badge:
  - Green if >= target margin
  - Yellow if within 5%
  - Red if below target by >5%
Allow user to:
- Edit final price per visit
- Toggle “round to nearest $5/$10/$25”

6) Good / Better / Best tiers (Commercial)
Generate 3 tiers automatically based on scope + add-ons:
- Basic Janitorial (baseline from walkthrough)
- Enhanced Sanitation (adds high-touch, disinfect, more frequent restroom detail)
- Premium Maintenance (adds glass, rotation tasks, floor care suggestions)
Each tier has:
- Scope summary bullets
- Frequency options (or same frequency w/ added tasks)
- Price per visit + monthly
User can edit tier names, included tasks, and prices.

7) Proposal Builder + PDF output
Create a “Commercial Proposal” preview screen:
- Cover: logo + customer + facility name
- Scope of Work (AI-generated draft + editable)
- Included / Not Included
- Schedule & Frequency
- Pricing Table (Good/Better/Best)
- Terms (payment terms, cancellation, access, supplies, insurance)
- Acceptance section (checkbox “Accepted” + signature placeholder if exists; otherwise simple “Accept Proposal” button that sets status=Accepted)
Export:
- Generate branded PDF and allow share via email/text like existing residential quote PDF

AI FEATURES (COMMERCIAL)
Add an AI button “Generate Professional Scope” which:
- Sends structured walkthrough data (NOT raw photos) + tier info to OpenAI
- Returns:
  - Scope of Work paragraph + bullet list
  - Included/Not Included
  - Suggested rotation tasks (weekly/monthly/quarterly)
- Must be editable before sending
Also add “AI Risk Scan” (optional button) that returns warnings like:
- Underpricing risk
- Unusual ratios (bathrooms vs sqft)
- Facility-type hazards (medical/restaurant)
- Suggested clauses

DATA MODEL
Create/extend Quote model to support:
- quoteType: "residential" | "commercial"
- commercialWalkthrough: object containing all fields
- laborEstimate: { rawMinutes, rawHours, recommendedCleaners, overrideHours?, targetMinutesPerCleaner }
- pricing: { hourlyRate, overheadPct, targetMarginPct, suppliesSurcharge, finalPricePerVisit, monthlyPrice, roundingRule }
- tiers: array of { name, scopeText, includedBullets[], excludedBullets[], pricePerVisit, monthlyPrice, frequency }
- status: Draft | Sent | Accepted | Declined
- createdAt, updatedAt
Ensure backward compatibility with existing residential quotes.

UI / UX REQUIREMENTS
- Commercial screens must feel modern and polished (spacing, cards, clear typography).
- Use a stepper/progress indicator for the walkthrough.
- Validate numeric fields and show friendly errors.
- Provide “Save Draft” at each step.
- Add a “Commercial Quote” badge on quote list items.
- Add filters on quote list: All / Residential / Commercial.

INTEGRATIONS
- Reuse existing customer records + contact fields.
- Reuse existing PDF generation pipeline; add a new PDF template for commercial.
- Reuse existing share/send mechanisms.
- Ensure no OpenAI calls happen without consent.

TESTING / QA CHECKLIST
- Create a commercial quote end-to-end without AI.
- Create a commercial quote with AI scope (consent modal appears first time).
- Decline consent -> AI buttons disabled, manual flow works.
- Switch quote types without losing residential functionality.
- Existing residential quote creation still works exactly the same.
- PDF generates correctly for commercial proposal.
- Quote status can be set to Accepted from proposal screen.

DELIVERABLES
- New components/screens for Commercial Walkthrough wizard, Labor Estimate, Pricing Engine, Tier Builder, Proposal Preview.
- Updated quote creation entry screen with Quote Type selection.
- Updated data model + storage serialization.
- New PDF template for commercial.
- AI service functions for scope generation + risk scan (with consent gate).
- Settings toggle + feature flag.

IMPLEMENTATION NOTES
- Keep code organized: /features/quotes/commercial/*
- Avoid giant files; create reusable components (StepCard, NumericInput, TierCard, MarginBadge).
- Add clear comments for where to customize the labor model constants.
- Make the defaults sensible for cleaning businesses.
- Do not remove or refactor existing residential logic unless necessary; isolate changes.

Now implement this feature end-to-end in the app.