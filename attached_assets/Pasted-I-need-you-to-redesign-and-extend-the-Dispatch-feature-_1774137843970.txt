I need you to redesign and extend the Dispatch feature in QuotePro so it becomes a real operational tool for cleaning businesses.

Current problem:
On the Dispatch card, the “Text Details” and “Email Details” actions are not good enough.
Right now, when the user clicks them, it just says “sent,” which creates confusion.

Major UX issue:
There is no clear recipient selection.
The user has no idea:
- who it was sent to
- where the number/email came from
- whether it was sent to a customer, cleaner, or someone else
- whether they can edit the destination before sending

This needs to be fixed properly.

Main goal:
Turn Dispatch into a powerful “share / dispatch / assign” workflow for business owners and ops managers.

What I want:
1. When a user clicks Text Details or Email Details, open a proper popup/modal
2. The popup should let the user choose who to send it to
3. The user should be able to:
   - enter a custom phone number or email manually
   - select from saved employees
   - select from customer contact info if relevant
4. The message should be previewed before sending
5. The user should confirm the send intentionally
6. Success feedback should clearly say who it was sent to

Example of better success message:
- “Dispatch details sent to Sarah at (484) 555-1212”
- “Job details emailed to mike@company.com”

This should feel polished, explicit, and trustworthy.

Big new feature to add:
I also want a new “Employees” section/menu in the product.

Purpose of Employees section:
Cleaning businesses need to store employee/team member info so they can:
- assign jobs
- share dispatch details
- text or email job info
- manage who is on the team
- prepare for future scheduling / dispatch workflows

What the Employees section should include:
- employee name
- phone number
- email
- optional role
- optional status (active/inactive)
- optional notes
- possibly color/avatar initials for future schedule assignment

Minimum requirements for Employees feature:
1. Add a new “Employees” menu item/section in the app
2. Build a basic employee management page
3. Allow users to:
   - add employee
   - edit employee
   - delete/archive employee
   - view employee list
4. Employee data should be reusable in Dispatch and job assignment workflows

Dispatch feature upgrades required:
I want Dispatch to support two strong use cases:

USE CASE 1: Share job details
- copy address
- copy full job details
- open in maps
- text details
- email details

USE CASE 2: Assign / dispatch to a team member
- choose an employee
- send dispatch details to that employee
- optionally assign the job to that employee during the same flow
- clearly show who the assigned employee is

Best UX direction:
Please redesign this so it feels intentional and operationally useful.

Recommended flow for Text Details / Email Details:
- user clicks button
- modal opens
- recipient section:
  - customer
  - employee
  - custom
- if employee chosen:
  - searchable dropdown from Employees list
- if custom chosen:
  - manual input field
- show message preview
- allow editing of message if useful
- send button
- show clear success/failure result

Employee integration ideas:
Please make the Dispatch modal smart:
- if employees exist, let the user pick from them quickly
- if no employees exist, show a clear CTA:
  - “Add your first employee”
- optionally allow:
  - “Send and assign job”
  - “Assign without sending”

Assignment requirements:
Since I mentioned assigning jobs too, please think through how to support that cleanly.

At minimum:
- when dispatching to an employee, allow the user to assign that employee to the job
- save the assignment to the scheduled job record if applicable
- reflect that assignment in the UI

Please inspect the codebase and determine:
- whether jobs already support employee/team assignment
- whether the current Dispatch actions are tied to customer/job data
- where to best add Employees in the navigation and data model
- how to make this reusable for future scheduling/operations features

Important:
Do NOT keep the current vague “sent” behavior.
Do NOT send messages without the user knowing exactly where they went.
Do NOT make this feel like a hidden magic action.

I want explicit, high-trust UX.

What I need you to build:
PART 1 — Dispatch modal overhaul
- Text Details opens modal
- Email Details opens modal
- recipient selection
- custom entry
- employee selection
- customer selection if available
- preview message
- clear send confirmation
- clear success message naming recipient

PART 2 — Employees section
- new Employees menu item
- employee CRUD page
- fields for name, phone, email, role, status
- reusable employee picker component if appropriate

PART 3 — Dispatch + assignment integration
- allow sending dispatch details to selected employee
- allow assigning job to employee from the same flow if the data model supports it
- store assignment properly
- show assigned employee on job detail if applicable

Message/content requirements:
Dispatch details should be formatted clearly and professionally.
Example content could include:
- customer name
- service address
- date
- arrival window
- service type
- price
- notes if available

Please generate the best clean operational format for:
- SMS dispatch
- email dispatch

UX requirements:
This needs to feel premium, fast, and useful.
No clunky forms.
No ambiguous actions.
No unexplained sends.

Think like an ops manager who needs to quickly send job details to a cleaner and know exactly what happened.

Implementation requirements:
1. Audit the current Dispatch feature
2. Find how Text Details and Email Details currently work
3. Replace the current vague send behavior with a real recipient-driven modal flow
4. Add Employees section and employee data model if needed
5. Connect Employees into Dispatch and job assignment
6. Reuse existing SMS/email infrastructure if possible
7. Add clear toasts/success states with recipient detail
8. Handle validation cleanly:
   - invalid phone/email
   - no recipient selected
   - no employees available
   - send failure

Please also think ahead:
Structure this so it supports future features like:
- dispatch to multiple employees
- route sharing
- access instructions / lockbox notes
- “on my way” texts
- team scheduling
- job assignment from calendar

Deliverables:
1. Explain the best UX for Dispatch + Employees in plain English
2. Show where Employees should live in the product nav
3. Show how the recipient selection flow should work
4. Implement the feature in the real codebase
5. Show exact files/components/functions changed
6. Explain how to test:
   - text to custom number
   - email to custom address
   - text/email to employee
   - assign employee to job
   - clear success/failure feedback

Success criteria:
- clicking Text Details or Email Details no longer blindly sends without recipient clarity
- users can choose exactly where the dispatch goes
- success messages clearly name the recipient
- the app has an Employees section for storing team members
- employees can be used for dispatch and job assignment
- the whole experience feels polished, premium, and operationally strong

Important:
Do not stop at analysis or mock UI.
Inspect the real code, implement the Dispatch modal overhaul, add the Employees section, wire it into assignment, and summarize exactly what changed.

Before coding, first propose:
1. the best Dispatch modal UX
2. the best Employees page structure
3. the best way to connect dispatching and assignment
Then implement it.