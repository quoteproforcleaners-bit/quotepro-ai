I have a Node.js + Express + TypeScript backend. All ~120 routes 
live in a single file: server/routes.ts (~14,800 lines). 
I need to split this into domain routers WITHOUT changing any 
route behavior, middleware logic, or business logic.

1. ROUTES SPLIT
   Create these new files, each as an Express Router:
   - server/routers/authRouter.ts — all /api/auth/* routes
   - server/routers/quotesRouter.ts — all /api/quotes/* routes
   - server/routers/customersRouter.ts — all /api/customers/* routes
   - server/routers/jobsRouter.ts — all /api/jobs/* routes
   - server/routers/pricingRouter.ts — all /api/pricing/* routes
   - server/routers/aiRouter.ts — all /api/ai/* routes
   - server/routers/automationsRouter.ts — campaigns, sequences, 
     growth automation, lead finder routes
   - server/routers/integrationsRouter.ts — QBO, Jobber, 
     Google Calendar, webhook, API key routes
   - server/routers/adminRouter.ts — all /api/admin/* routes
   - server/routers/publicRouter.ts — all /q/*, /r/*, /rate/* routes

   In server/routes.ts (now just a registry file), import and 
   mount each router:
   app.use('/api/auth', authRouter)
   app.use('/api/quotes', quotesRouter)
   etc.

   All middleware (requireAuth, requirePro, requireGrowth) must 
   be imported from a shared server/middleware.ts file.

2. DATABASE INDEXES
   Create a Drizzle migration that adds indexes on:
   - quotes.customerId
   - quotes.status
   - quotes.createdAt
   - quotes.deletedAt
   - jobs.quoteId
   - jobs.scheduledDate
   - jobs.status
   - customers.businessId
   - analytics_events.createdAt (for the TTL cron)
   - quote_follow_ups.quoteId
   - communications.customerId

3. ANALYTICS TABLE TTL
   In server/index.ts, add a cron job that runs every Sunday at 2am:
   DELETE FROM analytics_events WHERE created_at < NOW() - INTERVAL '90 days'
   Log the count of deleted rows.

4. FIX MIDDLEWARE NAMING
   In server/middleware.ts:
   - Rename requirePro to requireGrowth (it gates Growth features)
   - Create a new requirePro that gates actual Pro-tier features
   - Add a JSDoc comment explaining the tier hierarchy:
     free < starter < growth < pro
   - Update every route that uses these middlewares accordingly

5. SHARED PRICING ENGINE
   - Create shared/pricingEngine.ts with the canonical pricing logic
   - Update server/pricingEngine.ts to import and re-export from shared/
   - Update web/src/lib/pricingEngine.ts to import from shared/
   - Delete the duplicate logic so there is exactly ONE source of truth
   - Add a comment at the top of both files: 
     "Source of truth: shared/pricingEngine.ts"

Do not change any route behavior, response shapes, or business logic.
Test that the app starts and all routes respond correctly after the split.